# Implementation Discrepancies — KCPC R3.5

None affecting implementation so far. This register will grow as later phases surface genuine
ambiguities between governing documents. Format per build-prompt §1:

| ID | Affected document(s) | Requirement/section | Ambiguity/conflict | Impact | Work continues? | Proposed clarification |
|---|---|---|---|---|---|---|
| DISC-002 | `API_Specification.md` §17.2/§9 (`API-OP-019`: `PUT /api/v1/content-plans/{contentPlanId}/folder-link`, Permission #13 `PERM_13_FOLDER_LINK_MANAGE`); `UIUX_Design_Specification.md` §9.6 ("Content Asset Folder Link (Perm #13)") | Permission #13 is defined in the governed 17-permission catalogue (`OperationalPermission.PERM_13_FOLDER_LINK_MANAGE`) but is never actually checked anywhere in the codebase; `folderLink` is instead one field inside `PlanningService.updateParameters(...)`, gated on Permission #2 (Planning Execution) only, with no dedicated `/folder-link` endpoint. Discovered while building the Planning workspace MVC screen (`CRITICAL ARCHITECTURE RULE` / UI/UX §9.6 build pass) and confirmed pre-existing (Phase 4, already marked TESTED) - not introduced by this pass. | An Employee delegated Permission #2 can change the folder link without holding Permission #13 (over-broad); an Employee delegated *only* Permission #13 (a plausible narrow "folder-link manager" delegation) cannot use it at all (under-provisioned) - both are permission-model correctness gaps, not just cosmetic. The Planning workspace screen built in this pass renders/gates the folder-link field against the *actual* current behavior (Permission #2) so the UI does not lie about what the server enforces. | Splitting `updateParameters` into a separate `updateFolderLink` method + new `PUT /api/v1/content-plans/{id}/folder-link` REST endpoint gated on Permission #13 is a contract change to an existing, already-TESTED endpoint (`ContentPlanParametersRequest`/`API-OP-017/018`) and was judged out of scope for a UI-build pass per the Continuous Conformance rule against silently changing existing contracts; recommend a dedicated follow-up change (add the new endpoint, deprecate the bundled field, migrate the one caller). |
| DISC-001 | `API_Specification.md` §17.8 (Domain 8: Hold, Resume, Reschedule, Reassign, Cancel, Reopen Completed - `API-OP-047`..`053`) | `HTTP Method & Path` cards for all of Domain 8 specify `/api/v1/workflows/{workflowInstanceId}/...` | The shipped implementation (Phase 9-11, predating this pass) instead exposes every one of these operations under `/api/v1/content-plans/{id}/...` (`HoldRestController`, `AdminActionRestController`) - a resource-nested convention used consistently across the whole application, but never the literal path the frozen API spec states, and never previously recorded as an engineering decision. Discovered while implementing `CORR-001`/`ENG-019` (the reopen-publishing/reopen-performance split). This is not a spec ambiguity (the spec is unambiguous) but a real, systemic implementation-vs-contract deviation that predates this session. | Every Domain 8 endpoint is reachable and functionally correct at its current (non-literal) path; nothing is broken. The deviation is cosmetic to the URL only - request/response bodies, semantics, authorization, and workflow effects all match the spec. Not fixed in this pass: renaming all 8 endpoints is a cross-cutting, higher-risk change (breaks any existing caller/test/JSP form using the current paths) that is out of scope for `CORR-001` and warrants its own reviewed change. | Work continues on all unrelated items. | Either (a) formally amend the frozen API spec to document the `content-plans`-nested convention as the as-built contract (lowest risk, matches 100% of the shipped surface including Domains 1-7 which use the same convention throughout), or (b) schedule a dedicated path-migration pass across Domain 8 with route aliases kept during a deprecation window. Recommend (a) given every other domain in this codebase already uses the nested convention and the flat `workflows/{id}` form was never actually built anywhere. |
