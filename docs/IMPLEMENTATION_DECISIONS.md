# Implementation Decisions — KCPC R3.5

Ordinary engineering choices (build-prompt §4) recorded here, not as business rules.

| ID | Decision | Rationale |
|---|---|---|
| ENG-001 | Java 21 language level (`maven.compiler.release`), built/run on installed JDK 26 | Java 21 is the current LTS; Spring Boot 3.3.x targets it. JDK 26 is backward-compatible for `--release 21` compilation. |
| ENG-002 | Spring Boot 3.3.5, `war` packaging, executable via `java -jar` (embedded Tomcat) | JSP rendering under embedded Tomcat requires `war` packaging (Spring Boot docs); still runs as a single executable artifact for Docker, and remains deployable to an external servlet container if ever needed. |
| ENG-003 | Lombok omitted from entities/services so far (plain getters/setters) | Kept explicit for clarity while the domain model was still being worked out; may be introduced later for DTOs/mappers if boilerplate grows. |
| ENG-004 | `base_roles` (3 fixed access classes) modeled as a Java enum (`AccessClass`), not a JPA entity, though the DB table still exists and is FK-referenced by `business_roles` | It's fixed reference data with exactly 3 rows that never change; an enum is simpler and type-safe at the application layer while the DB table/FK/CHECK still enforces the physical contract. |
| ENG-005 | `User.businessRole` is `FetchType.EAGER` (ERD/JPA would default associations to LAZY) | `open-in-view` is disabled (correct practice); the resolved access class is read on nearly every authenticated request via `KcpcUserPrincipal`, built inside `JwtAuthenticationFilter` outside any open Hibernate session. EAGER avoids `LazyInitializationException` without resorting to OSIV. |
| ENG-006 | Timestamps (`created_at`, `updated_at`, event timestamps) populated via Hibernate `@CreationTimestamp`/`@UpdateTimestamp`, not relied upon from the DB `DEFAULT CURRENT_TIMESTAMP` | Hibernate always sends an explicit value in generated INSERT/UPDATE statements, so a DB-side `DEFAULT` never fires unless the column is also marked non-insertable - which would leave the in-memory entity stale after save. Column-level `DEFAULT CURRENT_TIMESTAMP` is kept in the DDL as a defensive fallback (e.g. for any future direct-SQL path) but the application never depends on it. |
| ENG-007 | `PermissionGrant.permission` (`OperationalPermission`) and `PermissionGrantStageScope.stageNumber` (`LifecycleStage`) use custom `AttributeConverter`s keyed on each enum's declared numeric code, not `@Enumerated` | The DB columns are `INTEGER` FKs into fixed lookup tables (`operational_permissions.permission_number`, `permission_grant_stages.stage_number`); `EnumType.ORDINAL` would silently write the wrong integer (Java enum declaration order) and `EnumType.STRING` would violate the column's INTEGER type entirely. |
| ENG-008 | `ip_address` columns (`user_sessions`, `system_audit_log`) mapped via `@JdbcTypeCode(SqlTypes.INET)` rather than `columnDefinition = "inet"` | Postgres's JDBC driver rejects an implicit `varchar -> inet` bind in a prepared statement; Hibernate 6's native `SqlTypes.INET` support binds correctly against the PostgreSQL dialect. |
| ENG-009 | Idea ID (`IDEA-YYYYMMDD-NNNN`) daily sequence derived via `COUNT(business_idea_code LIKE 'IDEA-YYYYMMDD-%') + 1` inside the submission transaction, not a dedicated locked sequence table | Unlike Content ID (`ERD-CON-038` mandates `SELECT ... FOR UPDATE` locking via `content_id_sequences`, `ERD-TBL-042`), the ERD defines no equivalent sequence table for Idea IDs, and the build prompt (§9) prohibits inventing new tables for convenience. At MVP scale (<15 users), the small race-condition window (two truly simultaneous same-day submissions could compute the same NNNN) is an accepted, disclosed risk rather than a schema deviation. |
| ENG-010 | `content_id_sequences` first-of-month row creation guards against an insert race (two concurrent transactions both finding no row for a new month) by catching the unique-constraint violation and re-reading under the row lock | `ERD-CON-038` mandates the FOR-UPDATE lock, but a lock can only be taken on a row that already exists; the very first allocation of a month has no row to lock yet. This closes that narrow window without weakening the locking guarantee for every subsequent allocation that month. |
| ENG-011 | Demo/seed data (`db/migration-demo/`) isolated into its own Flyway location, only added to `spring.flyway.locations` under the `dev` profile | Keeps the production/docker profile's schema exactly the governed physical model plus the one bootstrap CEO account - no demo accounts or sample data ever ship outside local development. |
| ENG-012 | GET `/api/v1/csrf` added as an unauthenticated priming endpoint that reads the request-scoped `CsrfToken` | `CookieCsrfTokenRepository` only writes the CSRF cookie when something actually resolves the deferred token (a JSP evaluating `${_csrf.token}` does this implicitly for browser flows); a pure JSON/REST client that never renders a JSP has no other way to obtain the cookie before its first unsafe request. This is the standard Spring Security pattern for REST/SPA CSRF priming - the token is not treated as a secret to the legitimate client (the cookie is deliberately JS-readable); the double-submit protection comes from same-origin policy, not token secrecy. |

| ENG-013 | "Start Shooting" (SA→SIP) and "Start Editing" (EA→ED), and submitting Shoot/Edit Review, are gated to native CEO/MM authority **or** an actively assigned Cameraperson/Editor on that Content Plan - not a CEO-Granted Operational Permission | The 17-permission catalogue has no permission for "begin work" or "submit for review" (only Assignment #4/#6 and Review #5/#7 exist); gating on active-assignee membership is the natural reading of BFD's SIP/ED status descriptions ("physically underway") and avoids inventing an 18th permission, which build-prompt §7 explicitly prohibits ("Strictly NO Permission #18 exists"). |
| ENG-014 | Shoot/Edit Review "qualifying final contributor(s)" are supplied explicitly by the reviewer as a recipient list at Approve time, validated against the recorded execution-participants snapshot taken at review submission | BRS-REQ-033/037 requires the reviewer to confirm which of the (possibly multiple) assigned contributors qualify as final for Mark attribution; the participants snapshot (`shooting_execution_participants` / `editing_execution_participants`, ERD-TBL-038/039) is both the self-review-conflict source of truth and the eligible-recipient allow-list, so a reviewer cannot attribute a Mark to someone who was never actually on the job. |
| ENG-015 | Planning Review approval performs a double workflow hop (PLRV→PLAP→SA) and Edit Review approval performs a double hop (ERV→EAP→RFP) inside one transaction, each recording two `WorkflowTransitionHistory` rows | BFD's status catalogue describes PLAP and EAP as immediately trigger the next state ("Planning Approved: ...triggers task activation in Shoot Assigned"; "Edit Approved: ...transitions to Ready for Publishing"), i.e. they are momentary pass-through states, not states the system waits in. Both hops are recorded for full audit fidelity rather than skipping the intermediate status. |

| ENG-016 | Scorecard draft/submit is hard-blocked before `performance_due_date` (rejects with `VALIDATION_FAILED`), rather than merely not triggering the PP→PFUP transition | Caught during golden E2E testing: allowing early entry while gating only the *transition* on due-date-reached left completion logic (which requires PFUP) unreachable after an early submission, stranding the deliverable at PP forever. Blocking early entry matches BFD's PFUP description ("Performance Due Date reached ... begins metric entry") read as a precondition, not just a trigger condition. |

| ENG-017 | `sessionManagement` is fully disabled (`AbstractHttpConfigurer::disable`) on both filter chains, rather than left at `SessionCreationPolicy.STATELESS`; the STATELESS-equivalent "no `HttpSession` touched" behaviour is instead configured explicitly via `RequestAttributeSecurityContextRepository` + `NullRequestCache` | **Bug found by the automated test suite, not by manual testing.** With `SessionCreationPolicy.STATELESS` alone, `SessionManagementFilter` still runs on every request; because our own `JwtAuthenticationFilter` sets a fresh `Authentication` per request (there is no login-form-triggered "authentication event" to distinguish first-login from every-subsequent-request), Spring Security's default `SessionAuthenticationStrategy` composite - which always includes `CsrfAuthenticationStrategy` whenever CSRF is enabled - treated *every single authenticated request* as a new login and rotated/deleted the CSRF cookie server-side in response. `GET /api/v1/auth/me` right after login would silently invalidate the CSRF token primed moments earlier, so the very next POST (e.g. logout) failed CSRF validation with 403. Manual `curl` scripts throughout this build never revealed this because each one re-read the cookie jar file fresh immediately before every POST; `TestApiClient` (and any real fetch/SPA client, or a JSP page reusing one primed token across multiple AJAX calls) holds the token in memory and reuses it, which is exactly the pattern the priming endpoint (`GET /api/v1/csrf`, ENG-012) exists to support. Setting `.sessionAuthenticationStrategy(new NullAuthenticatedSessionStrategy())` on `SessionManagementConfigurer` was tried first and did *not* work (`CsrfAuthenticationStrategy` is still independently appended); fully disabling `sessionManagement` was the fix that actually removes `SessionManagementFilter`, and therefore the trigger point, from the chain. Disabling it also silently reverts to the default `HttpSessionRequestCache`, which calls `request.getSession(true)` on every 401 to stash a `SavedRequest` - so `NullRequestCache` had to be added alongside it to avoid a new, equally unwanted `JSESSIONID` cookie. |
| ENG-018 | `V12__correction_ledgers.sql` adds a single reusable `trg_reject_update_delete()` trigger function attached to all 15 previously-unprotected append-only tables named in `ERD-CON-058` (plus the 3 new correction ledgers), rather than 15 bespoke trigger functions | Same guard logic (`RAISE EXCEPTION` on any `UPDATE`/`DELETE`) applies uniformly; a `DO $$ ... FOREACH ... $$` block attaches it table-by-table. `work_hold_records` is deliberately excluded - its "exactly one Resume transition" semantics are not simple append-only and need a dedicated trigger (tracked under DB-001). |
| ENG-019 | `AdminActionService.reopenCompleted`'s `PUBLISHING_REOPEN` target was corrected from `WorkflowStatus.RFP` to `WorkflowStatus.PUBG`, and the two spec-named endpoints `POST .../reopen-publishing` / `POST .../reopen-performance` (`API-OP-052`/`053`) were added alongside the existing generic `POST .../reopen` | Found while implementing `CORR-001`: `API_Specification.md` §17.8 states the workflow effect as `COMP -> PUBG` for `API-OP-052`, but the shipped code (predating this pass, Phase 9-11) transitioned to `RFP` instead - a genuine defect, not a spec ambiguity, so the code was fixed per this document's Continuous Conformance rule ("If the current code contradicts R3.5: fix the code"). The generic `/reopen` endpoint was kept (nothing depends on removing it) so no existing caller breaks. |
| ENG-020 | Path convention: the 3 new correction endpoints (`API-OP-033/041/046`) were implemented at the exact frozen paths (`/api/v1/ideas/{ideaId}/predefined-marks/corrections`, `/api/v1/publishing/events/{eventId}/evidence-corrections`, `/api/v1/performance/scorecards/{scorecardId}/corrections`); the two new reopen endpoints were added under the pre-existing `content-plans/{id}/...` convention rather than the spec's literal `workflows/{workflowInstanceId}/...` base | See `DISC-001` in `docs/IMPLEMENTATION_DISCREPANCIES.md` - the entire pre-existing Domain 8 (Hold/Resume/Reschedule/Reassign/Cancel/Reopen, Phase 9-11) already uses a `content-plans`-nested convention system-wide, undocumented until this pass. Renaming all of Domain 8 mid-way through `CORR-001` was judged higher-risk than valuable (breaks nothing today, but a wholesale REST surface rename affects every existing admin-action caller/test); recorded as a discrepancy instead of silently changing more of the contract than necessary. The 3 brand-new correction endpoints had no existing convention to conflict with, so they follow the frozen spec exactly. |
| ENG-021 | `V13`'s privilege-GRANT block (Part B) is existence/self-grant-guarded (`IF EXISTS (... kcpc_app ...) AND to_regrole('kcpc_app') IS DISTINCT FROM to_regrole(current_user)`) rather than assuming `kcpc_app` always exists as a distinct role | Local dev/test (`kcpc_dev`/`kcpc_test`) predates this session with a single `kcpc_app` role that is both the Flyway-connecting migrator and the (superuser) runtime role; the guard makes `V13` safely idempotent/no-op there instead of failing or granting a role privileges on itself, while still running for real in docker/prod where `db/init/01_create_app_role.sh` creates a genuinely separate `kcpc_app`. |
| ENG-022 | `db/init/01_create_app_role.sh` creates the restricted `kcpc_app` role via a shell script (`psql` heredoc) rather than a plain `.sql` file under `docker-entrypoint-initdb.d/` | Plain `.sql` init scripts have no environment-variable substitution; the role's password must come from `APP_DB_PASSWORD` (a Docker secret/env var), not be hardcoded into a committed file. A `.sh` script that shells out to `psql` with the variable already in its environment is the standard pattern for parameterized Postgres bootstrap. |
| ENG-024 | `ContentPlan.idea` and `WorkflowTransitionHistory.triggeredBy` changed from `FetchType.LAZY` to `EAGER` | Found by actually running the new MVC screens (§9.2/§9.15) against a live server, not just compiling: `pipeline.jsp` (`plan.idea.title`) and `deliverable-detail.jsp`'s timeline (`t.triggeredBy.fullName`) read these associations directly in the view, outside any open Hibernate session (`open-in-view` is deliberately disabled, ENG-005's precedent), throwing `LazyInitializationException`. Same fix pattern as `ENG-005` - narrow, targeted EAGER on the specific associations actually dereferenced in a view, not blanket OSIV. |
| ENG-023 | `V13` splits `work_hold_records`' DELETE-rejection (new dedicated trigger) from its UPDATE-lifecycle enforcement (existing `trg_work_hold_records_lifecycle` from V9) instead of one combined trigger | V9's trigger already correctly enforces the nuanced "exactly one Resume transition, then fully immutable" UPDATE rule (`ERD-CON-062` rules 6-8) and predates this pass; DELETE was simply never blocked (rule 9 was missed). Adding a second, narrowly-scoped `BEFORE DELETE` trigger closes the gap without touching or risking regression in the already-correct, already-tested UPDATE logic. |
| ENG-026 | New `MasterCatalogueService` + `MasterCatalogueRestController` (Platform/Channel/Target create/update/deactivate, Permission #17) implemented while building the `/app/admin/catalogue` MVC screen (UI/UX v0.2 §6.3), rather than deferred further | `API-OP-036`/`066`..`070` and the entire Permission #17 (`PERM_17_PLATFORM_CATALOGUE_MANAGE`) authorization check were entirely unimplemented (pre-existing gap, not introduced by this pass) - only read-only repository lookups existed internally, with no REST surface and no MVC surface. Since the governed UI screen requires these writes to function at all, closing the backend gap was a prerequisite for the screen, not optional scope creep. |
| ENG-027 | All 30 governed KPIs (`API-OP-058`) implemented via native SQL (`EntityManager.createNativeQuery`) rather than JPQL | JPQL cannot express Postgres date-diff/`EXTRACT(EPOCH FROM ...)` arithmetic or the stage-context `CASE` expression (`STAGE_PLANNED_DATE_CASE`, BR-039: each active deliverable's delay baseline is its *current* stage's planned date - Shoot/Edit/Live - not one fixed column) needed for the duration KPIs (021/025/027/028) and distribution KPIs (006/015/016/029). Non-obvious judgment calls, each grounded in the closest BFD wording: KPI-008/009 default to the current IST calendar month (BFD explicitly says "over monthly periods") when no `startDate`/`endDate` is given, KPI-010/011/022/023 default to all-time; KPI-013/014 "produced **and approved**" is read as "the output's parent plan passed Planning Review" (reached `SA` or later) since that is the concrete governed approval gate outputs pass through; KPI-021's turnaround is `decided_at - submitted_at` on every `review_cycles` row; KPI-027's is `first_completed_at - content_plans.created_at` (Planning starts when the Content Plan is created at Idea Approval); KPI-028's is the first `SAP` transition to the latest `ORIGINAL` publication event per Content ID; KPI-030 scopes "applicable" events to `ORIGINAL` only (a `REPOST` is not a first delivery, matching KPI-012's same `ORIGINAL`-only definition of "published"). |
| ENG-028 | `AdminReportingService.delayedDeliverables`'s Employee read-scope check treats **any** currently-valid `GLOBAL`-scope permission grant as full department-wide visibility, without evaluating `STAGE_RESTRICTED`/`ITEM_SPECIFIC` scope against each individual delayed row | `SRS-REQ-002` requires an Employee's view to include "deliverables falling within the scope of any active CEO-granted operational permission that authorizes acting on this resource." A fully faithful implementation would re-run the same per-item `scopeCovers` evaluation `AuthorizationService` already does for write actions, against every delayed deliverable, for every one of the employee's grants, on every report request - expensive and not yet built for a read-only reporting endpoint. `GLOBAL` scope is evaluated exactly (correct, no gap); `STAGE_RESTRICTED`/`ITEM_SPECIFIC` grant-holders currently see only their own assigned/participated deliverables on this report (an under-not-over-exposure gap - conservative, never leaks peer data, but may hide some in-scope items a narrowly-scoped delegate should see). Tracked as a known limitation, not silently ignored. |
| ENG-029 | `User.getPasswordHash()` marked `@JsonIgnore`; `AuditRestController` returns a new `AuditLogResponse` projection instead of the raw `SystemAuditLog` entity | **Security defect found and fixed while building the Audit Log endpoint (`API-OP-061`), not by manual review.** `SystemAuditLog.actor` is an EAGER `User` association (necessary for MVC/JSP rendering per ENG-005/ENG-024's pattern); returning the entity graph directly from a REST controller serialized the actor's bcrypt password hash straight into the JSON response. Every other controller in the codebase already avoided this by construction (`UserProfileResponse`, `AuditLogResponse`, etc. instead of raw `User`) - this was the first place a `User`-bearing entity graph was returned unprojected. `@JsonIgnore` on the getter is added as defense-in-depth so the same mistake anywhere else in the API surface fails safe instead of leaking; `ReportingApiSecurityTest` asserts no bcrypt-shaped string ever appears in a response body. |
| ENG-030 | `MultiFormatExportService` (`API-OP-062`) uses a fixed `Set<String> GOVERNED_TABLES` allowlist, validated *before* any value is interpolated into a `SELECT * FROM <table>` native SQL string, and reads generically via `JdbcTemplate.queryForList` (column-name-keyed maps) rather than per-table JPA entity/DTO mappings; XLSX and the full export payload are built fully in memory rather than true chunked/streamed generation | The allowlist is a hard SQL-injection guard - `tables` is caller-supplied and must never reach string concatenation unchecked. Generic `SELECT *` + column-map rows avoids hand-writing ~31 per-table export DTOs for a feature whose whole purpose is a raw physical-table dump (the ERD, not a curated API shape, *is* the contract here per RTM-081). In-memory buffering is an accepted simplification at the `SAD-ADR-009`/`010`-declared &lt;15-concurrent-user MVP scale; the response is still a single synchronous stream to the client as `SRS-REQ-080/081` require, just not internally chunked during generation. |
| ENG-031 | `KpiValue`, `AuditLogResponse`, and `DelayedDeliverableRow` implemented as plain classes with explicit `getX()` accessors, not Java `record`s, even though every other read-only DTO added this session (e.g. `MultiFormatExportService.ExportResult`) is a record | **Bug found by actually running the new report screens against a live server, not by compiling or unit-testing.** `/app/reports/kpis` and `/app/audit` both returned a misleading `302` redirect to `/login?reason=auth` - looking exactly like an authentication failure - when in fact classic JSP EL's `BeanELResolver` (used by `<c:forEach>`/`${...}` in these JSPs) only recognizes `getX()`-prefixed JavaBean accessors, not a `record`'s no-prefix canonical accessors (`displayValue()`, `eventTimestamp()`); the resulting uncaught `jakarta.el.PropertyNotFoundException` was caught by the security filter chain's exception handling and re-surfaced as an auth redirect, masking the real cause. Fixed by converting all three JSP-rendered DTOs to plain classes and updating every record-accessor call site (`k.code()` → `k.getCode()`, etc.) in `KpiService`/`ReportingMvcController`. `ExportResult` stays a record because it is never rendered by a JSP - only read in Java (`ExportRestController`). Rule going forward: any DTO dereferenced directly inside a JSP must be a plain class with `getX()` methods, never a record. |
| ENG-032 | `JwtAuthenticationFilter.shouldNotFilterErrorDispatch()` overridden to return `false` (re-run the filter on the container's internal `/error` forward, not just the original request); the dead `/app/admin/permissions` nav link in `pipeline.jsp` removed (permission administration is a read-through from `/app/admin/users/{id}`, per UI/UX v0.2 §6.2 - it was never a standalone screen) | **Real, user-reported bug: "clicking the Permissions tab logs me out."** Root cause was systemic, not specific to that one link: `OncePerRequestFilter` skips itself by default on Spring Boot's internal forward to `/error` (triggered by any unmapped URL's 404 or any uncaught exception's 500). With the filter skipped, no authentication is populated for that forwarded dispatch, `anyRequest().authenticated()` fails, and `MvcAuthEntryPoint` redirects to `/login?reason=auth` - indistinguishable from a real logout, and masking the true 404/500 status entirely (this is the same underlying mechanism that made the `ENG-031` bug's symptom look like an auth failure rather than a rendering exception). Verified live: before the fix, `curl`-ing an authenticated session against any unmapped `/app/**` path returned `302` to `/login`; after the fix, the same request correctly returns `404` and the session remains valid for a real screen immediately after. Regression-guarded by `AuthenticationFlowTest.unmappedAppUrlReturnsA404NotALoginRedirectWhileAuthenticated`. The immediate trigger (the dead nav link) was a genuine second bug layered on top - `/app/admin/permissions` was never wired to any `@GetMapping`, so it always 404'd, which the auth-dispatch bug then disguised as a logout. |
| ENG-033 | New `/app/admin/permissions` screen: a CEO-exclusive, read-only, all-users consolidated list of every currently active `PermissionGrant` (`PermissionGrantAdminService.listActiveGrants`, new `PermissionGrantSummary` plain-class DTO, `PermissionGrantRepository.findByActiveTrueOrderByEffectiveFromDesc`) | User request, after the dead `/app/admin/permissions` link (`ENG-032`) was removed: "can we keep a list of who currently has what permission, in one place" - the existing §6.2 read-through (open a user, see their grants) already answered "what does this one user have," but not "who across the whole org currently holds permission #N." Not a new governed screen in the frozen UI/UX spec (it extends the existing §6.2 read-through into an all-users view rather than introducing new business rules, fields, or a new permission), so implemented as a small additive read-only extension rather than a contract change - no new REST endpoint, no write path, same CEO-exclusive authority check (`requireCeo`) as every other permission-administration action. Grant/revoke *history* (not just current state) was already available via the Audit-History screen (`PERMISSION_GRANTED`/`MODIFIED`/`REVOKED` events, `ENG-029`'s reporting work) and needed no new work. `PermissionGrantSummary` follows the `ENG-031` plain-class-not-record rule since it is rendered directly by a JSP; grantee/grantor names and stage-scope labels are all resolved eagerly inside the service's `@Transactional` method, since `PermissionGrant.grantee`/`grantor` are LAZY associations that would otherwise throw `LazyInitializationException` when read from the JSP outside the session (`ENG-005`/`ENG-024` pattern). Regression-guarded by an assertion added to `AdminMvcScreenSmokeTest`. |
| ENG-034 | New `HighPriorityEdgeCaseTest` (5 tests) closing the specific gaps identified when asked "have you tested all scenarios": multi-contributor Mark attribution (two qualifying Camerapersons/Editors each get the FULL predefined Mark, never split), an expired delegated permission grant (403 `PERM_OPERATIONAL_PERMISSION_EXPIRED`), a revoked delegated permission grant, and a `STAGE_RESTRICTED` grant used outside its covered stage (403 `PERM_OPERATIONAL_PERMISSION_OUT_OF_SCOPE`) | A genuine, non-obvious finding while writing the revoked-grant test: `AuthorizationService.requireAuthority` fetches candidate grants via `findByGranteeAndPermissionAndActiveTrue` - a *revoked* grant (`active=false`) is therefore invisible to that query entirely, so it is correctly classified as `PERM_OPERATIONAL_PERMISSION_REQUIRED` ("never granted"), not `PERM_OPERATIONAL_PERMISSION_EXPIRED` - a distinct code path from a grant that is still fetched but whose `effective_until` has lapsed. Confirmed correct (not a defect) by first proving the same grant works before revocation, then asserting the exact post-revocation error code, so the test cannot silently pass for the wrong reason. All 5 scenarios passed against the real implementation with no code changes required elsewhere. |
| ENG-035 | Shoot/Edit Assignment converted from single-select `<select>`+"Assign" to a searchable, checkbox-backed multi-select shown as removable chips (`assignment-picker.js`, reusing the Model(s) picker's CSS/UX exactly - `model-picker.js`), with new remove endpoints (`.../shooting-assignments/remove`, `.../editing/assignments/remove`, both MVC and REST); new **Publishing Assignment** feature added to match (new `publishing_assignments` table/`PublishingAssignment` entity/`PublishingAssignmentRepository`, `PublishingService.assignPublisher`/`removePublisher`, new MVC+REST endpoints, `AssignPublisherRequest` DTO) | Explicit user request: give all three per-stage assignment pickers (Shoot/Edit/Publishing) the same UX as the existing Model(s) picker, filtered strictly by Business Role (never Designation - there is no Designation field in this codebase), with Active-only, role-matching employees, instant no-reload chip add/remove, and pre-selected chips on reload. **Governance deviation, explicitly confirmed with the user before implementing:** `publishing_assignments` has no ERD-TBL number in the frozen ERD (`Entity_Relationship_Diagram_and_Data_Dictionary.md`, current max is ERD-TBL-044) and none of the new remove/assign endpoints have an `API-OP-xxx` number in the frozen `API_Specification.md` - this is a new table/business capability, not a bug fix or spec-conformance correction. Scoped deliberately narrow to limit the deviation: Publisher Assignment carries **no new permission** (reuses `PERM_08_PUBLISHING_EXECUTION`, the same authority `startPublishing` already requires) and is purely additive tracking - it does **not** gate `startPublishing` or anything else, exactly like Shoot/Edit assignment don't gate Start Shoot/Start Edit either (`ENG-013`: those gate on assignee-membership-or-native-authority, independent of the assignment record's presence). `PlanningService.assignCameraperson`/`EditingService.assignEditor` were made idempotent (a second call for an already-actively-assigned user returns the existing row instead of inserting a duplicate) rather than changed in signature, so every pre-existing caller (both MVC and REST, single-id, called repeatedly to build up multiple assignees - see `HighPriorityEdgeCaseTest`, `GoldenEndToEndFlowTest`, `WorkflowVariantsE2ETest`, `CorrectionLedgerFlowTest`, `DbIntegrityEnforcementTest`, `CeoPipelineDashboardTest`, `MvcScreenSmokeTest`, `PlanningSingleFormTest`) keeps working unmodified; the MVC batch-add form instead adds a **new** plural parameter (`cameramanUserIds` etc.) alongside the legacy singular one. `V15__publishing_assignments.sql` also grants the restricted `kcpc_app` role the same non-append-only privilege level (`SELECT/INSERT/UPDATE/DELETE`) `V13`'s one-time privilege split (Part B) already gave `shooting_assignments`/`editing_assignments`, since `V13` only enumerated tables that existed when it ran and a table created later (this one) would otherwise have no grant at all under the restricted role in docker/prod. Note: `V15__publishing_assignments.sql`'s own inline comment still says `ENG-033` - it was already applied (checksum-locked) to local dev/test databases under that number before this numbering was corrected to `ENG-035` here, and Flyway migration files are never edited post-apply (checksum mismatch), so the stale reference was deliberately left as-is rather than silently diverging the applied file from its checksum. |
| ENG-036 | Team Lead added to Shoot and Edit Assignment (not Publishing): a new `is_lead` boolean column on `shooting_assignments`/`editing_assignments` (`V16__assignment_leads.sql`), a DB partial unique index enforcing at most one active Lead per Content Plan per stage, `PlanningService.setShootLead`/`EditingService.setEditLead` (null clears), new MVC+REST endpoints, and a `<select>` dropdown next to each chip-picker whose options are exactly the plan's current active Assignee(s) | Explicit user request, with the exact UX/permission/scope tradeoffs confirmed up front: Lead on Shoot+Edit only (not Publishing), one boolean flag per assignment row rather than a separate `lead_user_id` column (chosen over the user's own suggested shape because it fits the existing one-row-per-assignee schema exactly - removing an assignee's row already excludes it from every "current Lead" lookup with zero extra cascade code, whereas a separate id column would need manual clearing logic), and reuse of the existing Assign permission (`PERM_04`/`PERM_06`) rather than a new one. Same governance deviation as `ENG-035`: `is_lead` is not in the frozen ERD. The Lead `<select>` is populated purely from the server-rendered active-assignee list and kept in sync client-side by `assignment-picker.js` as chips are added/removed, so a Lead selection is always a subset of Assignee(s) by construction, both initially and reactively - never an independently-entered id. |
| ENG-037 | `/app/my-work`'s Shoot task list (`LandingMvcController.myWork`) now filters out any `ShootingAssignment` whose Content Plan is still at status `PL` or `PLRV`; Publishing tasks (previously entirely missing from My Work) added alongside the existing Shoot/Edit lists, via a new `PublishingAssignmentRepository.findByPublisherAndActiveTrue` | Explicit user request/bug report: an assigned Cameraperson was seeing the deliverable in My Work the moment they were assigned, which happens *during* Planning (Stage 3) itself, before Planning Review even starts - i.e. before Planning is actually Approved. Per the user: task visibility must come from an active assignment, never Designation/Business Role alone, **and** not before Planning Approval. Edit/Publishing assignment needed no equivalent filter: `EditingService.assignEditor`/`PublishingService.assignPublisher` already hard-reject assignment before Shoot Approval / Ready for Publishing respectively, so an active row for either can never exist before its own approval gate has passed - the Shoot case is the one path where assignment happens strictly earlier than the visibility should. Regression-guarded by `MyWorkVisibilityTest`, which asserts the Content ID is absent from the assigned Cameraperson's `/app/my-work` response body while still `PL`, then present once the same plan reaches `SA`. |
| ENG-038 | `/app/my-work`'s Assigned Tasks now only include a stage while it's still that employee's *active* window (Shoot: `SA`/`SIP`/`SRV`; Edit: `EA`/`ED`/`ERV`; Publish: `RFP`/`PUBG`); once the plan moves past that window, the task drops out of Active and appears instead in a new read-only "My Completed Work / History" section (`CompletedWorkItem`: Content ID, Stage Worked, Completed On, Final Result - Shoot/Edit's Final Result from the deciding `ReviewCycle`, Publish's left blank since Publishing has no review/decision gate) | Explicit user request: once an employee's own stage is done (reviewed/approved) and the plan has moved on, it should no longer sit in their active work list - a Cameraperson whose Shoot was Approved and is now in Edit shouldn't keep seeing it as "active," nor should they see any next-stage operational detail, only their own stage's outcome. "Completed On" is computed generically for all three stages as the most recent `WorkflowTransitionHistory` row whose `fromStatusCode` was inside the active window and `toStatusCode` was outside it (not tied to the review cycle specifically), so a reopen-then-reclose cycle (`ENG-019`'s reopen mechanism) is reflected correctly by always using the latest such exit rather than the first. Completed rows carry no link back to `/app/deliverables/{id}` - deliberately, since that page currently has no access restriction of its own (any authenticated Employee can open it by URL regardless of assignment) and linking there would defeat the "no next-stage detail" intent; flagged separately to the user as a bigger, not-yet-scoped follow-up. Regression-guarded by `MyWorkVisibilityTest.camerapersonTaskMovesFromActiveToCompletedHistoryOnceShootIsApproved`. |
| ENG-039 | `deliverable-detail.jsp`'s Editor Assignment section (chips, checklist, and Edit Lead dropdown) is now shown at `status == 'SAP' or status == 'EA'`, not `'SAP'` alone | **Real bug found from a user screenshot**, not by review. `EditingService.assignEditor` already auto-transitions `SAP -> EA` the instant the *first* Editor is assigned (pre-existing behavior, not introduced by `ENG-035`); the JSP's own visibility condition never accounted for that, so on any page reload after the first assignment the whole section - not just the "+ Assign"/"Set Lead" buttons - silently disappeared, even though `EditingService.assignEditor`/`setEditLead`'s own guards (`status != SAP && status != EA`) already explicitly allow assigning further Editors (or changing the Lead) at `EA`. Confirmed live against the user's exact reported plan: its Timeline showed `SAP -> EA (ASSIGN_EDITOR)` already recorded, and re-fetching that page showed no Editor picker at all. The one-editor-at-a-time old `<select>`+button design had the same latent gap but it went unnoticed because that design always full-page-reloaded per action, so status had already visibly moved on and nobody expected the form to still be there; `ENG-035`'s AJAX, no-reload picker made the gap reachable in a single sitting (both Editors could be added without a reload) but then broke on the next real reload. Regression-guarded by `AssignmentPickerTest.editAssignmentPickerAndLeadDropdownStayVisibleAndUsableAfterFirstEditorMovesStatusToEA`, which reproduces the exact sequence: assign Editor 1 (triggers `SAP -> EA`), reload, assert the picker is still present, then assign Editor 2 while already at `EA`. |
| ENG-040 | `assignment-picker.js` (Shoot/Edit/Publishing chip-pickers) changed from instant-save-per-checkbox to stage-then-confirm: checking a box only builds a local chip (`data-staged="true"`, no network call); the now-always-visible "+ Assign" button (relabeled "Assign Cameraperson(s)"/"Assign Editor(s)"/"Assign Publisher(s)") batch-saves every currently staged chip in one AJAX call on click; a staged chip's own × just un-stages it locally (nothing to undo server-side yet), while an already-saved chip's × still removes it via a real (still no-reload) AJAX call exactly as before. The Lead `<select>` follows the same pattern - choosing an option only stages the value, "Set Lead" persists it. The button is disabled until at least one chip is staged | Explicit user request/UX correction: after choosing an Editor, an explicit "Assign Editor" confirmation button was expected, not a silent per-checkbox save - the earlier `ENG-035` design (instant AJAX on every check) is what this replaces. This does not abandon the original "chip appears the instant you select it" requirement from earlier in the same feature - the chip still appears immediately (optimistic/staged), only the actual persistence is now deferred to the explicit button click, batching every staged chip in one call. This also makes the three pickers structurally consistent with `publication-scope.js`'s pre-existing "+ Add Target" pattern (check boxes, one button saves the batch) rather than diverging from it. No server-side change was needed - the MVC batch-add endpoints already accepted a plural id list (`ENG-035`), so the redesign is entirely client-side; verified live (headless Chrome, this repo has no in-suite browser-test tooling - see `ENG-035`'s same caveat) that staging/un-staging fire zero network requests and only the confirmed chip is persisted, with an un-staged-then-removed chip never reaching the database at all. |
| ENG-041 | Shoot/Edit Assignment's two buttons (`ENG-040`'s "Assign Cameraperson(s)"/"Assign Editor(s)" add-form plus the separate "Set Lead" lead-select-form) are merged into one. The Lead `<select>` now lives inside the same `.assignment-add-form` as the checklist (JSP: chips+search sit above the form, the checklist and Lead `<select>` sit inside it as the two `.assignment-picker-grid` columns, one submit button below both - no nested `<form>`s, since chips/search have no `name` attributes needed for submission and stay outside). One click now assigns every staged Cameraperson/Editor and sets the Lead together in a single POST to new `POST .../shooting-assignments/team` / `POST .../editing/assignments/team` MVC endpoints, backed by new `PlanningService#assignShootTeam` / `EditingService#assignEditTeam` - each simply calls the existing `assignCameraperson`/`setShootLead` (or `assignEditor`/`setEditLead`) methods in a loop-then-lead sequence from inside one new `@Transactional` method; self-invocation on the same Spring bean bypasses the inner methods' own `@Transactional` proxies, so the whole thing commits or rolls back as one database transaction, not two sequential AJAX calls under one button. The Lead `<select>`'s `name` changed from the singular assignee-param name (`cameramanUserId`/`editorUserId`) to `leadUserId` to avoid colliding with the checklist's plural field now that both live in the same form. Publishing is untouched (no Lead concept, no second button to merge). The older `/shooting-assignments`, `/shooting-assignments/lead`, `/editing/assignments`, `/editing/assignments/lead` endpoints from `ENG-035`/`ENG-036` are left exactly as they were (still used by `AssignmentPickerTest`'s existing REST/legacy-MVC coverage) - the new `/team` endpoints are additive, not a replacement of the underlying single-purpose methods. No REST (`/api/v1/...`) parity added for the combined endpoint - it exists purely to serve this one button; any API client that needs the same effect can already call the two existing REST endpoints in sequence. | Explicit user request with a UI mockup: "Dono select karne ke baad ek hi button: Assign Editor(s). Click par ek hi transaction/request me: selected Editors assign hon, selected Editor ka is_lead = true ho, baaki selected Editors ka is_lead = false. Page reload na ho; success ke baad chips + Lead state wahi update ho." - the two-button flow (`ENG-036`'s Lead dropdown bolted on next to `ENG-035`'s add form) read as two unnecessary separate actions once both existed side by side. Regression-guarded by `AssignmentPickerTest.shootTeamSingleRequestAssignsCamerapersonsAndSetsLeadTogether` (assign+lead in one call, then a lead-only re-submit with no new assignees proves the "other selected assignees' `is_lead` = false" half), `AssignmentPickerTest.shootTeamInvalidLeadRollsBackTheNewlyStagedAssigneeToo` (an invalid Lead in the same request 400s and the newly-staged Cameraperson is NOT left assigned either - proves the single-transaction/atomicity requirement specifically, not just the happy path), and `AssignmentPickerTest.editTeamSingleRequestAssignsEditorsAndSetsLeadTogether`. Verified the JSP compiles and the full suite (72 tests) stays green after the merge. |
| ENG-042 | Two further corrections to `ENG-041`'s merged Shoot/Edit Assignment picker, both from a live screenshot: (1) **layout** - the Cameraperson(s)/Editor(s) chip+search field and the Shoot/Edit Lead field are now the two columns of `.assignment-picker-grid` itself (JSP: the grid is the direct parent of both the chips/search `<div>` and `.assignment-add-form`; `.assignment-add-form` gets `display: contents` in CSS so its own box doesn't consume a grid cell - its children, the Lead field/checklist/button, become grid items in their own right, letting the checklist and button span both columns via `grid-column: 1 / -1` on the row(s) below - this keeps chips' own tiny `<form>`s from ever nesting inside `.assignment-add-form`, which HTML forbids). (2) **immediate Lead options** - `assignment-picker.js`'s Lead `<select>` previously only listed already-*saved* assignees (`ENG-040`/`ENG-041`'s design), so a just-checked, still-staged chip never showed up as a pickable Lead until after the "Assign ...(s)" button was clicked; new `refreshLeadOptions(picker)` fully rebuilds the Lead options from **every currently visible chip, staged or saved** (`getSelectedChips`), called on every stage/un-stage/remove, so the Lead list now updates live as you check boxes - and by rebuilding from scratch and only restoring the previous selection if it's still in the new list, "Lead auto-clears to None when their chip is removed" and "existing Lead pre-selected on reload" both fall out for free with no separate bookkeeping (this also let the old manual `removedLeadOption`/incremental-`buildLeadOption` bookkeeping in the remove-chip and submit handlers be deleted in favor of one `refreshLeadOptions()` call at each point of change). | User's exact report: "editor or lead ek hi grid line m show ho or editor select karne k baad lead m list nhi aa rhi h" (with a UI mockup showing the two fields side by side and a worked example of 3 selected editors all appearing in the Lead dropdown), plus explicit pseudocode for `refreshLeadOptions` matching what was implemented (full options rebuild from the current selection set, preserving the current value only if still valid). Verified with a static-fixture Puppeteer repro (`app.css` + `assignment-picker.js` loaded against the actual new JSP markup, no server needed) confirming: the two `.assignment-picker-field` boxes render on the same row with the Lead field immediately to the right (`getBoundingClientRect` top values equal, Lead's left >= Cameraperson's right); checking a box adds that name to the Lead `<select>` immediately, before any network call; un-staging the currently-selected Lead's chip removes its option and the select's value reverts to `""` (None) automatically; and the final "Assign Cameraperson(s)" click leaves the chosen Lead selected with the button correctly disabled again afterward (nothing left to save). Full suite (72 tests) re-verified green - this change is JSP/CSS/JS only, no server-side behavior changed. |
| ENG-043 | CEO/Marketing Manager's native authority (`AuthorizationService.hasNativeAuthority`) no longer satisfies the hands-on **execution** acts - Start Shoot, Submit Shoot Review, Start Edit, Submit Edit Review, Start Publishing, and (newly) recording an Actual Publication - only an actively assigned Cameraperson/Editor/Publisher on that specific Content Plan can perform them now. `ShootingService`/`EditingService`'s existing `requireAssigneeOrNative` helper (ENG-013: gated to native CEO/MM **or** an active assignee) is renamed `requireActiveAssignee` with the `hasNativeAuthority` early-return deleted outright - so it's now assignee-only, full stop. `PublishingService` gains a brand-new `requireActiveAssignee` (Publishing execution had **no** assignee requirement at all before this - only `PERM_08_PUBLISHING_EXECUTION`, see `ENG-035`'s original "purely additive, does not gate startPublishing" note, now superseded), called in `startPublishing` and `recordActualPublication` *in addition to* the existing PERM_08 check - so a non-native Employee needs both a PERM_08 grant and to be the assigned Publisher, and CEO/MM auto-pass the permission check via native authority but still get blocked by the new assignee check. Every other action in all three services - Assignment (assign/remove/set-lead), Review decisions (`decideShootReview`/`decideEditReview`/Planning Review), Target N/A, evidence corrections, Hold/Resume, Reassign, Cancel - is untouched; CEO/MM's native authority still covers all of that exactly as before. MVC-side, `DeliverableMvcController`'s model attributes were renamed to match (`isShootAssigneeOrNative`→`isShootActiveAssignee`, `isEditAssigneeOrNative`→`isEditActiveAssignee`, both dropping the native-authority `\|\|`) plus a new `isPublishActiveAssignee`; `deliverable-detail.jsp`'s Start Shoot/Start Edit buttons switched to the renamed flags, and the Publishing panel's "Start Publishing" and "Record Actual Publication Event" blocks are now additionally gated on `isPublishActiveAssignee` (Publisher assignment, Target N/A, and evidence correction stay on `canPublishingExecute` alone, unaffected). **Consequence worth flagging**: Publisher assignment (`ENG-035`, previously purely optional/additive tracking) is now *effectively* required before anyone can execute Publishing - a Content Plan reaching `RFP` with no Publisher assigned is unstartable by anyone, including CEO/MM, until CEO/MM (who retains the Assign action) assigns one. This mirrors how Shoot/Edit assignment was already effectively mandatory (SA/EA can't be reached without at least one active Cameraperson/Editor), so it brings Publishing in line rather than introducing a new class of problem, but it's a real behavioral tightening for any pre-existing RFP-status plan with no Publisher assigned. | Explicit user request, in the same session as `ENG-036`'s original deferred question ("Start Edit button visibility restricted to only the assigned Editor, not CEO/MM") - now answered definitively and extended to all three stages: "CEO / Marketing Manager ko assigned execution task ke Start aur Submit/Complete buttons show nahi hone chahiye. Unka role planning, assignment, review aur monitoring ka rahe... Important: sirf UI se buttons hide mat karna. Backend authorization bhi enforce karo, taaki CEO/MM direct endpoint call karke task start/submit na kar saken," with an explicit rule: "Execution action allowed only when: User has required permission + active matching assignment + user is an execution employee for that assignment. CEO/MM inherent management authority ≠ execution authority." This is a natural, symmetric follow-on to `ENG-013`'s original assignee-or-native gate - only the "or-native" half is removed, and Publishing (which never had this concept) is brought up to the same standard as Shoot/Edit rather than left as the odd one out. Regression-tested by fixing every existing test that exercised Start/Submit/recordActualPublication as the CEO account under the old native-authority bypass (`CeoPipelineDashboardTest`, `CorrectionLedgerFlowTest`, `DbIntegrityEnforcementTest`, `GoldenEndToEndFlowTest`, `HighPriorityEdgeCaseTest`, `MvcScreenSmokeTest`, `PlanningSingleFormTest`, `WorkflowVariantsE2ETest`, `AssignmentPickerTest`) to instead log in as the actively-assigned Cameraperson/Editor/Publisher for those specific calls, while leaving every management/review call on those same tests as CEO, unchanged - proving the split is enforced exactly at the intended boundary, not weakened to make tests pass. Full suite (72 tests) green; packaged build verified clean. |
| ENG-044 | Publisher(s) assignment (the chip-picker, its checklist, "Assign Publisher(s)" button, and both `PublishingService#assignPublisher`/`#removePublisher`) is now restricted to native CEO/MM authority only - `authorizationService.requireNativeAuthority(actor, "Publisher assignment")` replaces the old `requirePublishingAuthority` (PERM_08) check in both methods, so holding a `PERM_08_PUBLISHING_EXECUTION` grant is no longer sufficient to assign, reassign, or remove a Publisher on any plan. A rank-and-file Publisher's PERM_08 grant now exists for exactly one purpose - executing their own assigned task (`ENG-043`'s `startPublishing`/`recordActualPublication`, unaffected here) - never to manage who else is assigned. MVC-side, `DeliverableMvcController` gains `canAssignPublisher` (= native authority only, reusing the already-computed `nativeAuthority` local); `deliverable-detail.jsp`'s Publishing panel now has two independent blocks under `status == 'RFP'` instead of one shared gate: the assignment picker under `canAssignPublisher`, and "Start Publishing" (unchanged from `ENG-043`) under `canPublishingExecute and isPublishActiveAssignee` - so a Publisher sees only their own "Your Assignment / Start Publishing" card, never the picker, while CEO/MM see only the picker (they're never an assignee themselves in the normal case). Target N/A and evidence correction stay on `canPublishingExecute` alone (PERM_08, CEO/MM native), unaffected - out of scope for this request. | Explicit user follow-up, same session as `ENG-043`: "Publisher user ko sirf apna assigned work execute karna chahiye. Usko Publishing Assignment, Publisher(s) picker, Assign Publisher(s) button ya kisi aur Publisher ko add/remove karne ka option show nahi hona chahiye... Kisi doosre Publisher ka assignment ya work modify nahi kar sake... Backend authorization bhi same rule enforce kare; sirf UI hide karna enough nahi hai," with an explicit target screen (Publisher sees only "Your Assignment" + "Start Publishing", then post-start only their own Publication Targets + "Mark Published"; "Assign Publisher(s) sirf CEO/MM ... ko dikhe; Publisher ko nahi"). Necessary because `ENG-043` left Publishing's only permission (`PERM_08_PUBLISHING_EXECUTION`) doing double duty for both assignment and execution - unlike Shoot/Edit, which already have a genuinely separate frozen-catalogue permission for assignment (`PERM_04`/`PERM_06`) distinct from their assignee-gated execution, so no equivalent fix was needed there. Regression-guarded by `AssignmentPickerTest.assignedPublisherWithPerm08CanExecuteButCannotTouchPublisherAssignment` (a Publisher holding both an active assignment AND a PERM_08 grant - everything `ENG-043` alone would have allowed - still gets 403 attempting to assign a second Publisher or remove their own assignment, while their own `Start Publishing` call still succeeds and transitions the plan to `PUBG`) plus the pre-existing `publishingAssignmentOnlyValidAtRfpAndRequiresPublishingPermission`'s "no PERM_08, not native" 403 case (comment updated - the actual server-side reason flipped from "no PERM_08 grant" to "not native authority", same observable 403 either way). Full suite (73 tests) green; packaged build verified clean. |
| ENG-045 | Planning's Shoot Assignment (Cameraperson(s)/Shoot Lead) no longer has its own "Assign Cameraperson(s)" button or dedicated save endpoint call - it's folded into the single "Submit for Planning Review" action, exactly like Model(s) already worked. JSP: the picker's checkboxes and the Lead `<select>` now sit outside `<form id="planning-details-form">`'s DOM boundary (still inside their own `.kcpc-model-picker.planning-shoot-picker` block, positioned where the old picker was - after Planned Outputs, before the submit button) but carry the HTML5 `form="planning-details-form"` attribute, so they submit together with it when the external button (already `form="planning-details-form"`) is clicked - verified live in a real browser (Puppeteer) that the POST body correctly bundles Planning Details fields + `cameramanUserIds` (repeated) + `leadUserId` in one request. The picker itself switched from `assignment-picker.js` (AJAX-per-action, `ENG-040`-`ENG-042`) to `model-picker.js` (the plain "stage locally, submit natively with the form, no separate save step" behavior Model(s) already had) - `model-picker.js` gained new optional `.kcpc-lead-select` awareness (`refreshLeadOptions()`, rebuilding the Lead's options from whichever checkboxes are currently checked on every change, mirroring `assignment-picker.js`'s `ENG-042` logic but simpler since there's no staged-vs-saved distinction here - everything is just "currently checked or not"), a no-op for the unrelated Model(s) field which has no Lead select. Server-side, new `PlanningService#savePlanAssignAndSubmit` composes `savePlan` + `reconcileShootTeam` + `submitPlanningReview` via self-invocation (bypassing their own `@Transactional` proxies, same technique as `assignShootTeam`) into ONE transaction - if any step fails, including the submit-readiness check, NOTHING is saved, not even the Planning Details fields, reversing the previous two-transaction design ("Plan saved, but not submitted" is gone; the flash message is now "Nothing was saved: ..."). New `reconcileShootTeam` (distinct from `ENG-041`'s additive-only `assignShootTeam`, which the standalone chip-picker's own dedicated add/remove endpoints still use unmodified) actively **removes** any active Cameraperson assignment not present in the submitted set, not just adds new ones - unchecking someone in the form is now how you remove them, taking effect only once the whole form is submitted, per the user's explicit "chips form state me rahen... final assignment officially tab create ho jab Submit for Planning Review click ho." A new `touchShootAssignment` flag (computed server-side from the SAME `PERM_04_SHOOT_ASSIGNMENT` check that gates whether the JSP even renders the fields, not from request-shape inference) guards the whole reconcile step - without it, a PERM_02-only preparer who never had the Cameraperson(s) fields on their page at all would either have their submission wrongly blocked by a PERM_04 error from the unconditional `setShootLead` call, or - worse, since `reconcileShootTeam` removes anyone not in the submitted set - have every existing Cameraperson assignment silently wiped by an empty implicit submission. The older `/shooting-assignments`, `/shooting-assignments/team`, `/shooting-assignments/lead` MVC/REST endpoints are untouched and still fully functional for any other caller (API clients, `AssignmentPickerTest`'s existing coverage) - only the JSP's own Planning-screen picker moved off them. | Explicit user request: "planning wali screen per assigned camera person ka button dene ki need nhi jab m sbmit planning karu tab jenhe choose kiya gya h wo us form k saath submit hojaye... Alag assignment button remove karo... Aur sab ek hi transaction me hona chahiye. Agar assignment save fail ho, to planning review submit bhi nahi hona chahiye... agar planner sirf values fill kar raha hai aur abhi review submit nahi kiya, to Cameraperson chips form state me rahen. Final assignment officially tab create ho jab Submit for Planning Review click ho." The "ideal flow" list the user gave (Planning Details + Models + Planned Outputs + Cameraperson(s) + Shoot Lead → Submit) was read as describing the page's existing top-to-bottom section order, not a request to also fold Planned Outputs' own independent add/remove/publication-scope flow into this - nothing in the message described how that would even work, unlike the specific, concrete instructions given for Cameraperson(s)/Shoot Lead, so Planned Outputs was deliberately left untouched. Regression-guarded by three rewritten `PlanningSingleFormTest` cases - `combinedSubmitRollsBackEverythingWhenSubmissionIsRejected` (renamed from `combinedSubmitStillSaves...`, now asserts the opposite: a rejected submit leaves `categoryText`/`plannedLiveDate` null, proving the new atomic rollback), `nativeAuthorityPreparerCanDecideTheirOwnPlanningReview` and `planningWorkspaceShowsNoSaveButtonAndOneFinalSubmitReferencingTheTopForm` (both updated for the new single-POST shape/markup) - plus one new test, `combinedSubmitReconcilesCamerapersonsToExactlyWhatsChecked` (seeds a stale Cameraperson via the still-live standalone endpoint, then submits with only a different one checked, and asserts the stale one is gone and only the fresh one remains active). Full suite (74 tests) green; packaged build verified clean; the cross-form-boundary submission mechanism itself was additionally verified live via a static-fixture Puppeteer repro (real Chrome, actual `model-picker.js`/`app.css`) confirming the exact POST body shape a browser sends. |
| ENG-046 | Two new, related features added to each of Shoot/Edit/Publishing Assignment: (1) a **Description** - one shared free-text value per Content Plan per stage (not per individual assignee), stored as three new nullable `TEXT` columns directly on `content_plans` (`shoot_description`/`edit_description`/`publishing_description` - a new table was deliberately avoided since each stage maps 1:1 to a ContentPlan and it's a single mutable value, not a growing list; "retained through stage transitions" falls out for free since a column is never deleted). Editable any time (not restricted to a workflow-status window) by whoever holds Shoot/Edit Assignment authority (`PERM_04`/`PERM_06`) or, for Publishing, native CEO/MM only (matching `ENG-044`'s Publisher-assignment rule, since Publishing's only permission does double duty and a rank-and-file Publisher's `PERM_08` must stay execution-only). New `PlanningService#updateShootDescription`/`EditingService#updateEditDescription`/`PublishingService#updatePublishingDescription`, one new MVC endpoint each (`POST .../shooting|editing|publishing/description`, AJAX-aware, no reload), rendered as its own small `<textarea>` + "Save Description" mini-form in the Shoot/Edit/Publishing panels (not folded into `ENG-045`'s combined Planning submit - Description is explicitly a standalone, always-updatable field, a different requirement from that bundling). (2) **Comments** - a new, genuinely separate `stage_comments` table (`content_plan_id`, `stage`, `commenter_user_id`, `comment_text`, `created_at`) giving each stage its own permanent, append-only Jira-style discussion thread - Shoot/Edit/Publishing threads never mix, reusing `LifecycleStage`'s existing SHOOTING/EDITING/PUBLISHING values (CHECK-constrained at the DB layer) rather than a parallel enum. New cross-cutting `com.kcpc.mkt.discussion` package (`StageComment` entity, `StageCommentRepository`, `StageCommentService`) since this spans all three stages, not owned by any single existing service. Comment authorship is native CEO/MM authority or whoever is currently an active assignee on that exact stage - the same split `ENG-043`/`ENG-044` established for execution actions, reused here for who may speak on a stage's thread; reading a thread is not separately gated, matching the deliverable-detail page's existing (page-level, not per-section) visibility model. `stage_comments` gets the same append-only DB enforcement as every other audit-adjacent table (`trg_reject_update_delete`, `ERD-CON-058`/`ENG-018`'s pattern) and the matching restricted `SELECT, INSERT`-only grant (not the `SELECT/INSERT/UPDATE/DELETE` level the assignment tables get) - nobody, not even the author, can edit or delete a comment; it's permanent history by construction, not merely by convention. `StageComment.commenter` is `FetchType.EAGER` (`ENG-005`'s established pattern - rendered directly in the JSP outside any open Hibernate session, `open-in-view` disabled) - found via a live `LazyInitializationException` in the very first test run, not by inspection. AJAX comment-add returns the saved comment's `commenterName`/`createdAt` (IST-formatted via the existing `DisplayTime.ist()` utility)/`commentText` as JSON so the client can append the exact server-recorded values rather than guessing the commenter's own display name or a client-clock timestamp. `my-work.jsp`'s Assigned Tasks table gained a Description column (the relevant stage's value per row) so an assigned employee sees their instructions without opening the deliverable. Both Description and Comments intentionally live in the Shoot/Edit/Publishing execution panels (status-gated SA/SIP/SRV, SAP/EA/ED/ERV, RFP/PUBG/PP) rather than the Planning panel, even though Cameraperson(s) assignment itself now happens during Planning (`ENG-045`) - keeps them decoupled from `ENG-045`'s heavily-bundled single-submit flow and matches where "Start X"/"Submit for X Review" already live. Not in the frozen ERD/API spec. | Explicit, highly detailed user request across three follow-up messages, each answering the natural next question before it was asked: per-stage separation ("Shoot Description → Cameraperson ko shoot-related instructions... har stage ka Description alag-alag hona chahiye"), atomicity with the assignment ("Assignment save hote hi description bhi us stage assignment ke saath save ho"), no-reload editing ("Edit/update without page reload ho"), My Work visibility ("Assigned employee ke My Work me ye Description clearly visible ho"), a textarea over a single-line input ("instructions/update kabhi long ho sakte hain"), and for Comments: a Jira-ticket-style layout with an explicit worked example (CEO/Rohan/Marketing Manager exchange), append-only ("edit/delete ideally normal employees ko freely allow mat karo; audit history retain ho"), stage isolation ("Shoot comment Edit thread me mix na ho"), no-reload posting, and an explicit suggested backend shape ("stage_assignment → assignment data + description", "stage_comments → content_id + stage + user_id + comment + created_at") that this implementation follows closely (description on the assignment-adjacent `content_plans` row per stage; comments in their own dedicated table). Regression-guarded by new `StageDiscussionTest` (4 cases): `shootDescriptionRequiresPerm04AndPersists` (CEO can set/update it repeatedly, the assigned Cameraperson without a `PERM_04` grant cannot, proving "authorized assigner" ≠ "the executing employee"), `shootCommentsRestrictedToActiveAssigneeOrNativeAndPersistWithCommenterAndTimestamp` (CEO and the active Cameraperson can both comment, an uninvolved outsider gets 403, persisted rows carry the right commenter/order), `shootAndEditCommentsNeverMixAcrossStages` (a Shoot comment and an Edit comment on the same plan land in their own separate stage-scoped lists), and `publishingDescriptionIsNativeOnlyButAssignedPublisherCanComment` (the assigned Publisher is blocked from setting the Description - native-only - but can still comment on their own stage's thread). Full suite (78 tests) green; packaged build verified clean. |
| ENG-047 | Revises `ENG-046`'s Shoot Description placement: it's now also entered directly on the Planning screen, alongside Cameraperson(s)/Shoot Lead (relabeled "Shoot Instructions" there and in the Shoot panel - same field, clearer name; Edit/Publishing keep "Description"). JSP: a `<textarea name="shootDescription">` sits inside `.planning-shoot-picker`, right after the Cameraperson(s)/Shoot Lead grid, carrying `form="planning-details-form"` exactly like the Cameraperson(s) checkboxes/Lead `<select>` already do (`ENG-045`) - so it submits in the same single POST, no separate save step. Server-side, `PlanningService#savePlanAssignAndSubmit` gained a `shootDescription` parameter and, inside its existing `touchShootAssignment` guard, now also calls `updateShootDescription` (self-invocation, same transaction as `reconcileShootTeam`/`submitPlanningReview` - a failure anywhere still rolls back everything, unchanged from `ENG-045`). This is an ADDITIONAL entry point, not a replacement: `ENG-046`'s standalone AJAX-editable field in the Shoot panel (`PlanningService#updateShootDescription` called directly, its own `/shooting/description` endpoint) is untouched and still there for CEO/MM to update later - both read and write the same `shoot_description` column, so there's no divergence between them, just two places in the UI to touch the one value. Edit/Publishing Description are unaffected - the user was explicit that only Shoot moves ("Ye Shoot-specific description ho; Edit aur Publishing ke descriptions baad me unke respective assignment stages par separately add hon"). | Explicit user follow-up, same session as `ENG-046`: "Cameraperson/Shoot Description add karna bilkul sahi rahega, especially kyunki Cameraperson(s) aur Shoot Lead bhi isi planning screen par select ho rahe hain... Planning approve hone ke baad wahi description Shoot Assignment instructions ban jaye... is field ka naam simply Shoot Instructions rakhna Description se bhi clearer hoga," with an explicit layout (Cameraperson(s) | Shoot Lead, then a full-width Shoot Description row) and flow ("Planning → Cameraperson(s) + Shoot Lead + Shoot Description → Planning Review → Approved → Shoot Assignment"). Makes sense given `ENG-045` already moved Cameraperson(s)/Shoot Lead selection itself into Planning - instructions for that same team belong right next to it. Regression-guarded by new `PlanningSingleFormTest.combinedSubmitAlsoSavesShootInstructions` (submits `shootDescription` together with Cameraperson(s)/Shoot Lead/Planning Details in one `/plan-submit` call, asserts it persists on `ContentPlan.shootDescription` and the "Shoot Instructions" label renders). Full suite (79 tests) green; packaged build verified clean. |
| ENG-048 | Shoot Instructions (the `ENG-046`/`047` shared per-plan Shoot Description) and its Comments thread are now ALSO shown on the Planning Review Decision screen (`status == 'PLRV'`), not only during Planning (`PL`) or later in the Shoot panel (`SA`/`SIP`/`SRV`) - same markup/endpoints as the Shoot panel's existing block, just a third place the same `shoot_description`/`stage_comments` data appears, gated the same way. `PlanningService#updateShootDescription`'s authorization is broadened: previously PERM_04 (Shoot Assignment) or native CEO/MM only, now ALSO accepts PERM_03 (Planning Review) via new private `requireShootDescriptionAuthority` (tries PERM_04 first, falls back to PERM_03, matching the "either authority" shape) - a Planning Approver can correct the Cameraperson team's instructions at approval time without separately needing Shoot Assignment authority. `DeliverableMvcController`'s `canEditShootDescription` model flag is broadened identically (PERM_04 OR PERM_03) so the UI's read-vs-edit decision stays consistent with what the server will actually accept. Every edit's audit entry (`SHOOT_DESCRIPTION_UPDATED`) now carries the old and new value in its `actionReason` (`"Old: \"...\" -> New: \"...\""`, the OLD value captured before the field is overwritten) rather than `null` - visible immediately in the existing `/app/audit` Logs page (its `Reason` column already renders `actionReason`, so no new UI was needed) with "who" already covered by that same page's `Actor` column - satisfies "Edit silently overwrite na ho — audit trail me old + new value aur kisne change kiya, save ho" without inventing a new audit-detail mechanism. | Explicit user follow-up, same session as `ENG-046`/`047`, prompted by a live screenshot of the Planning Review screen showing neither the instructions nor a way to discuss them: "jo planning aprover h unko us task k regarding description bhi show ho or wo change bhi kar sake ya use regarding comment kar sake... Approver description dekh sake. Authorized Approver description edit/update bhi kar sake. Edit silently overwrite na ho — audit trail me old + new value aur kisne change kiya, save ho... Planning approve hone ke baad final Shoot Instructions Cameraperson ke My Work me exactly wahi show hon" (already true - unaffected, `my-work.jsp` already reads the same `shootDescription` field). The "use Comment instead of editing Description for a mere doubt" guidance and "Request Rework only for planner-level corrections" are UX conventions for the approver to follow, not something enforced in code - both the edit and comment paths stay available regardless, matching how this app doesn't otherwise police *which* legitimate action a user picks. Regression-guarded by new `StageDiscussionTest.planningApproverWithPerm03CanEditShootInstructionsDuringReviewWithAuditedOldAndNewValue` (grants a fresh Employee PERM_03 only, no PERM_04; walks a plan to PLRV with an initial Shoot Instructions value; confirms the reviewer's page shows it, their edit succeeds and persists, and the resulting `system_audit_log` row's `actionReason` contains both the old and new text with `actor` correctly attributed to the reviewer, not the CEO who set the original value). Full suite (80 tests) green; packaged build verified clean. |
| ENG-049 | UI-polish pass over the Planning/Shoot/Edit Review Decision screens and the `ENG-046`/`048` Description/Comments blocks (Shoot Instructions, Edit/Publishing Description, Comments): (1) Decision/Reason moved into a `.review-decision-grid` (side by side, Submit Decision compact/right-aligned, not full-width) and the Reason field is now conditionally HIDDEN (not merely optional) unless "Request Rework" is selected, via `review-decision.js` toggling both `required` and a `.hidden` class on the field's wrapping `<label>`. (2) Description read view defaults to plain text + a small "Edit" button (`.stage-description-header`, a heading+button row separate from the read-only text/edit-form so Edit sits next to the heading instead of floating below the paragraph) instead of an always-open textarea; clicking Edit swaps to the textarea + Cancel/Save, all client-side via `stage-discussion.js`'s `wireDescriptionBlock` (no reload). (3) Comments restyled flat/divided (Jira-style, `.stage-comment` with a bottom rule) instead of boxed cards, and the "No comments yet." placeholder was later removed outright (an empty thread just shows nothing). (4) Button hierarchy: Submit Decision stays the one primary/red action; Save Instructions/Cancel/Comment are secondary/outline and compact (shared `padding:0.4rem 0.9rem; font-size:0.85rem` rule); the standalone Edit trigger went through two further passes - first a small outline button (`padding:7px 14px`), then (per an explicit "jaise Comment ka button dikh raha h wase hi" follow-up) unified onto that exact same shared secondary-button styling as Comment/Cancel/Save - and finally restyled again as a quieter pencil-icon text-link (`✎ Edit`, transparent/borderless, muted gray, darker+underlined on hover) rather than a bordered button at all, sized via `button.stage-description-edit-btn` (an element+class selector, needed only because `.panel button`/`.action-form button` - an existing element+class rule - would otherwise outrank a bare `.stage-description-edit-btn` class selector and repaint it button-red). (5) The three block headings (Shoot Instructions/Edit Description/Publishing Description, and Comments) were resized to match the Decision label's typography exactly (`font-size:0.85rem; font-weight:600; line-height:1.3`) instead of the browser's larger default `<h3>` styling, via `.stage-block-heading` - Shoot/Edit/Publishing Description's heading now lives inside `.stage-description-header` alongside Edit (no longer a lone sibling `<h3>`), so the `:first-of-type`-based tighter-top-margin rule from the first pass was dropped in favor of giving `.stage-description-header` that spacing directly (Comments, still a standalone `<h3>`, keeps the looser default gap). Not in the frozen ERD/API spec (styling only, no schema/endpoint changes). | Sequence of explicit user follow-ups in the same session as `ENG-046`/`047`/`048`, each triggered by a live screenshot: an ASCII-mockup-backed request to divide the Review Decision screen into three clear blocks and make Reason conditional, Instructions default-to-read-mode, and Comments read like a real thread ("current UI functional hai, but... hierarchy thodi flat lag rahi hai... Reason ko sirf Request Rework select hone par show karo... texarea hamesha editable dikhane ke bajay pehle clean text show karo... No comments yet ki jagah proper discussion area banao"); a follow-up removing the now-redundant "(mandatory for Request Rework)" qualifier text once Reason was already conditionally hidden; a follow-up matching Shoot Instructions/Comments heading size to the Decision label and asking for the Edit button to be "thoda or optimize karo" (compact, right-of-heading, not floating below the text); "No comments yet dikhane ki need nahi hai" (drop the empty-state placeholder entirely); "jaise comment ka button dikh raha h wase hi css edit button ko bhi kar do" (unify Edit's sizing with Comment's); and finally a mockup-plus-explanation preferring a pencil-icon text-link over a button ("Edit ko normal red button ki jagah small subtle text action rakho... Main pencil icon + Edit text recommend karunga, sirf icon nahi"). Per the user's standing "skip UI verification" feedback (pure CSS/JSP/JS changes), no test suite run or manual browser verification was performed for this pass - the user tests these themselves. |
| ENG-050 | Comment authors may now edit or soft-delete their OWN comments via a Jira-style "..." menu (Edit/Delete), shown only on a comment where `commenter.id == the viewing user's id` - reversing part of `ENG-046`'s original "nobody, not even the author, can edit or delete" append-only rule, per an explicit later user request. Narrower than a full reversal in two ways: (1) hard DELETE stays impossible at the DB level (`trg_stage_comments_reject_delete` on `stage_comments` is untouched by `V18__stage_comment_edit_delete.sql`; only `trg_stage_comments_reject_update` is dropped, since edit/soft-delete both require a real `UPDATE`) - a delete sets a new `is_deleted` flag + `deleted_at` timestamp and leaves the row and its last text in place, so "history retained, nothing hard-deleted" holds structurally, not just by convention; (2) it is deliberately NOT bypassable by CEO/MM's usual native authority - `StageCommentService#requireOwnComment` checks `comment.getCommenter().getId().equals(actor.getId())` with no native-authority escape hatch, unlike every other authority check in this app, matching the user's explicit "Sirf apne comment par... show ho." New `StageComment.editedAt`/`deleted`/`deletedAt` columns/fields (`editText()`/`softDelete()` mutators) and two new service methods (`editComment`, `deleteComment`), each audited via the existing `AuditService` with the same "Old: \"...\" -> New: \"...\"" (edit) / "Deleted text: \"...\"" (delete) `actionReason` shape `ENG-048` established for Shoot Instructions edits - visible on the existing `/app/audit` Logs page, no new audit UI needed. Six new thin MVC endpoints (`POST .../{shooting,editing,publishing}/comments/{commentId}/edit|delete`, AJAX-aware) delegate to two shared private controller helpers (`doEditComment`/`doDeleteComment`) since editing/deleting only needs the `commentId` - `StageCommentService` looks the comment up directly and checks ownership, not the stage - so the business logic isn't tripled across the three per-stage routes the way `addComment` necessarily is (it needs the caller-supplied `LifecycleStage` to know which thread to post into). The add-comment AJAX JSON response gained a `commentId` field (previously just `commenterName`/`createdAt`/`commentText`) so a freshly-posted comment can be wired with its own Edit/Delete menu immediately, without a reload, exactly like a page-loaded one would have. UI: an inline "..." dropdown (`.stage-comment-menu`, closes on outside click or another menu opening) reveals Edit (swaps the read text for a textarea + Cancel/Save) or Delete (swaps it for a small inline "Delete this comment? This cannot be undone." confirm + Cancel/Yes, not a native `confirm()` dialog, which would break the no-reload AJAX flow); an "(edited)" marker is appended next to the timestamp after a successful edit. Both `stage-discussion.js`'s `wireCommentMenu` (server-rendered comments) and `buildCommentEntry` (a just-posted one) build/wire the identical DOM shape. Like `ENG-049`'s Description edit-toggle, this reveal-via-menu interaction is JS-required (the edit/delete forms are `hidden` by default with no `<noscript>` fallback) - a plain-form fallback would need the edit/delete affordance to be reachable without a click-to-reveal menu at all, which the user's explicit "..." request rules out; without JS, the underlying data is still fully reachable by reloading the page after any AJAX action elsewhere on it (nothing about this feature is unrecoverable without JS, just less convenient). Not in the frozen ERD/API spec. Regression-guarded by two new `StageDiscussionTest` cases (`commentAuthorCanEditOwnCommentAndItIsAudited`; `onlyCommentAuthorCanEditOrDeleteAndDeleteIsSoftNotHard` - proves CEO's native authority is rejected on someone else's comment, the author's own edit/delete succeed, the deleted row's text/id survive in the DB, and a second edit attempt on an already-deleted comment 400s) and one new `DbIntegrityEnforcementTest` case (`stageCommentHardDeleteIsStillRejectedAtTheDatabaseLevelAfterEng050` - a raw JDBC `DELETE` against `stage_comments` still fails with "append-only" after `V18`). Full suite (83 tests) green; `mvn compile`/`test-compile` verified clean. | Explicit user follow-up, same session as `ENG-046`-`049`, with a full worked mockup: "own comment par direct Edit / Delete text dikhane ke bajay ... menu better rahega... Sirf apne comment par ... show ho. Hover par ... thoda highlight ho. Edit inline ho, page reload na ho. Delete par small confirmation ho. Deleted comment ko hard-delete karne ke bajay audit/history retain karna better hai." Directly narrows `ENG-046`'s original append-only design (justified there as permanent, audited history for accountability) to allow self-correction of one's own words while preserving that same accountability goal through soft-delete + audit trail rather than true immutability - the user's own phrasing ("audit/history retain karna better hai") frames this as a refinement of the original goal, not an abandonment of it. |
| ENG-051 | Planning's "Submit for Planning Review" (`#planning-details-form`, `POST .../plan-submit`) previously always did a plain (non-AJAX) form POST - any validation failure, whether an obviously-missing field or a backend-only business rule, forced a full-page redirect (`return redirect(id)`) that discarded every field the user had filled in, including ones not yet persisted. New `src/main/resources/static/js/planning-submit.js` adds two layers: (1) client-side `validatePlanningForm()` blocks the request entirely (no network call, no navigation) when Planned Live Date is empty (always required) or, when Planning Mode is Urgent, when Shoot Date/Edit Date/Urgency Reason are empty - a rule a static HTML `required` attribute can't express since it depends on another field's live value. `form.noValidate = true` is set (JS-only) so the browser's own constraint-validation bubble for Planned Live Date's existing `required` attribute doesn't show inconsistently alongside the custom `.field-error` messages the other three rules need anyway - the plain `required` attribute itself is left in the markup, so the no-JS fallback still gets basic protection on that one field. Each failing field gets a `.field-error` message inserted directly after it (`field.insertAdjacentElement('afterend', ...)`) and an `.input-error` class (red border); the first invalid field is focused and scrolled into view. (2) Once client-side validation passes, the submit goes through `fetch` (not a native form POST) - `DeliverableMvcController#savePlanAndSubmit` gained the same `isAjax(requestedWith)` branching every other endpoint in this controller already uses: on success, `ResponseEntity.ok().build()` (JS then does `window.location.reload()` - a full reload is still correct on success since the plan transitions PL -> PLRV and most of the page's panels change, unlike the failure path this whole change is about protecting); on a caught `DomainException`, a JSON `ApiErrorResponse` (still prefixed `"Nothing was saved: "`, matching the non-AJAX flash message's existing wording) instead of a redirect, which the client shows as an inline banner at the top of the form rather than losing the page. `new FormData(form)` (not a manual field walk) is used to serialize the request specifically because it correctly includes the Cameraperson(s)/Shoot Lead/Shoot Instructions controls that sit outside this form's DOM subtree via the `form=""` cross-boundary attribute (`ENG-045`), same technique `publication-scope.js`'s `serializeForm` already relies on. Progressive enhancement preserved: with JS disabled, `#planning-details-form` is untouched and still POSTs/redirects exactly as before this change. Not in the frozen ERD/API spec (validation/UX only, no schema change). | Explicit user report with a screenshot and a detailed expected-behavior spec, prompted by hitting exactly this bug live: "jab maine aada form fill kar liya or ek required fild rah gayi or jab m submit kartu hu to error aati h or saath page referesh hota h jis se fill kiya hua data hat jata h... form submit prevent ho, page refresh na ho, jo data user ne fill kiya hai wo same rahe, missing field ke niche inline error message aaye, first invalid field par focus/scroll ho," plus an explicit worked implementation pattern (`clearValidationErrors`/`validatePlanningForm`/`showFieldError`/`focusFirstInvalidField`, `.field-error`/`.input-error` CSS) and an explicit "Important" callout not to rely on frontend validation alone: "Agar frontend pass ho gaya but backend validation fail hui, tab bhi full-page redirect karke entered values lose nahi hone chahiye... AJAX/JSON response se return karo aur wahi inline messages show karo," which is what the `savePlanAndSubmit` AJAX branch above implements. The three named validation rules (Planned Live Date always; Shoot/Edit Date + Urgency Reason for Urgent; at least one Reel Type for a Reel output) match this app's existing server-side rules exactly - `validatePlanningForm` mirrors, not invents, business logic already enforced in `PlanningService`. The third rule (Reel Type) applies to the separate "+ Add Output" form (`.add-output-form`, `POST .../outputs`), not `#planning-details-form` - that form was already fully AJAX-driven with no page reload at all (`publication-scope.js#handleAddOutputSubmit`), so it didn't share this bug and inline field-level validation for it is left as a smaller follow-up, not bundled into this fix. |
| ENG-052 | Shoot/Edit Review Decision's "Confirm qualifying Cameraperson(s)/Editor(s) (Approve only)" field - previously a native `<select multiple size="4">` sitting on its own line below Decision, disconnected from the rest of the form - is now a searchable chip multi-select (`.qualifying-picker-field`, reusing the exact `.kcpc-model-picker`/`.kcpc-model-input`/`.kcpc-model-chips`/`.kcpc-model-checklist`/`.model-check-item` markup and `model-picker.js` wiring the Planning "Model(s)"/"Cameraperson(s)" pickers already use - no new picker JS needed, only reuse) living in the SAME `.review-decision-grid` second-column slot Reason already occupies, and mutually exclusive with it: Approve shows the picker and hides Reason, Request Rework shows Reason and hides the picker - both toggled by the same `review-decision.js#sync()` that already drove Reason's ENG-049 conditional visibility, now extended to also toggle `.qualifying-picker-field`. `.review-decision-grid`'s `align-items` changed from `end` to `start` so the "Decision" and "Qualifying Cameraperson(s)/Editor(s)" column headings land on the same line (the picker column is taller than a plain Reason `<input>`, so bottom-alignment no longer reads as a matched header row). New client-side rule: submitting with Decision = Approve and zero qualifying-recipient checkboxes checked is blocked (`event.preventDefault()`) with an inline `.field-error` message on the picker field (reusing `ENG-051`'s `.field-error`/`.input-error` classes) rather than letting an empty-recipient approval reach the server at all; Request Rework has no such requirement, matching existing server-side behavior which never required a recipient there either. The underlying `<input type="checkbox" name="qualifyingRecipientUserIds">` list (unchanged field name/semantics from the old `<select multiple>`, still submits as a repeated form param) and the POST target/backend handling are untouched - this is presentation and pre-submit validation only, not a new business rule. Not in the frozen ERD/API spec (UI/UX only). | Explicit user request with a screenshot showing the disconnected native multi-select and a full worked layout: "ek hi grid line m rakho or multiple select kar k approve hoga jis se jin ko select kiya h unko mark mil sake or request rework karne per jo comment aa raha h wo bhi align hona chahiye... Normal native multiple-select list ki jagah searchable chip multi-select use karo... jaise hi Request Rework select ho: Qualifying Cameraperson field hide/disable ho, uski jagah Reason / Rework Comment same grid position me aaye... Ek important rule bhi rakho: Approve tab tak submit na ho jab tak kam se kam 1 qualifying Cameraperson select na ho." Applied symmetrically to both Shoot Review Decision (Cameraperson(s)) and Edit Review Decision (Editor(s)) per this session's established convention of keeping the two twin flows in lockstep, even though the screenshot only showed Shoot - Edit Review Decision gained the identical picker plus a new "Each confirmed contributor receives the FULL predefined Editor mark (no split)" note-box mirroring Shoot's existing one, for the same informational parity. Verified live against the running dev server (not just read - the exact rendered HTML for both a Shoot and an Edit Review Decision page was fetched and inspected) rather than assumed correct from the source alone, given two earlier CSS changes this session turned out not to render as expected from static reading; full regression suite (`MvcScreenSmokeTest`, `HighPriorityEdgeCaseTest`, `GoldenEndToEndFlowTest`, `WorkflowVariantsE2ETest` - all of which post `qualifyingRecipientUserIds` directly to these endpoints, bypassing the JSP form entirely) stayed green since the field name/semantics didn't change, only the picker's HTML/JS presentation. |
| ENG-053 | The Qualifying Cameraperson(s)/Editor(s) picker (`ENG-052`) could show the same person twice (or more) after a Shoot/Edit Review Request Rework cycle. Root cause: `shooting_execution_participants`/`editing_execution_participants` (ERD-TBL-038) are write-once-per-cycle rows, not a current-membership table - `ShootingService#submitShootReview`/`EditingService`'s equivalent insert a fresh participant row for every currently-active assignment EVERY time "Submit for Shoot/Edit Review" runs, and Request Rework returns the workflow to SIP/EIP (not back to SA/EA), so the same assignee can resubmit for review without ever being reassigned - producing a second row for that same person. `DeliverableMvcController#view` previously passed `shootingParticipantRepository.findByContentPlan(plan)`/`editingParticipantRepository...` straight into the model; now both go through a new private `dedupeByUser(rows, extractor)` helper (keeps the first row seen per `User.id`, a `LinkedHashMap<UUID, T>`) before reaching the JSP, per the user's explicit "Backend me better fix karo... userId ke basis par DISTINCT/deduplicate karo... main fix backend source par hona chahiye." Deduping by user id (not name, not row identity) is deliberate - two different people who happen to share a display name must never collapse into one entry, and two rows for the same person naturally do. Scoped narrowly to the two `model.addAttribute` calls building the picker lists - `shootingParticipantRepository.findByContentPlan(plan)` is called a second, independent time elsewhere in the same method for the unrelated `isShootParticipant` self-review-block check (a boolean `anyMatch`, unaffected by duplicates either way) and was deliberately left untouched. The backend approve path (`ShootingService#decideShootReview`) already deduped `qualifyingRecipientUserIds` into a `Set<UUID>` before creating Mark attributions, so mark-crediting was never actually double-counting from this bug - it was a picker-list *display* bug only, confirmed by reading that method before concluding the fix belonged at the list-building source rather than in the attribution logic. No frontend defensive dedupe was added on top (the user offered it as optional, backend was called out as the main required fix) - keeping the single source of truth at the model-building step avoids a second place this rule could drift out of sync. Not in the frozen ERD/API spec (bug fix only, no schema/endpoint change). | Explicit user bug report with a screenshot showing "Priya Nair / Ananya Verma / Priya Nair / Ananya Verma" in the Edit Review Decision's Qualifying Editor(s) checklist: "har stage m request rework karne per qualifying list m user k name doble bar aa rhe h... Likely issue ye hai ki rework ke baad same employee ke multiple assignment records fetch ho rahe hain aur UI un sabko render kar rahi hai... Employee uniqueness user_id se decide ho, name se nahi... Preferably current/relevant active assignment set se list aaye... Backend me better fix karo." The user's own root-cause hypothesis (multiple assignment/history rows accumulating across rework cycles) matched exactly what reading `ShootingService#submitShootReview` confirmed. Regression-guarded by new `HighPriorityEdgeCaseTest.qualifyingCamerapersonListNeverShowsDuplicatesAfterAReworkCycle` (Start Shoot -> Submit for Shoot Review -> Request Rework -> Submit for Shoot Review again for the SAME Cameraperson, no reassignment in between; fetches the rendered Shoot Review Decision page and asserts the Cameraperson's checkbox/data-name marker appears exactly once) - verified this test genuinely catches the bug by temporarily reverting the `dedupeByUser` fix, confirming the test fails with "expected: 1 but was: 2" (matching the exact duplicate-count pattern from the screenshot), then restoring the fix and confirming it passes again. Full suite (84 tests) green; packaged build verified clean. |
| ENG-054 | Reassign's "New Assignee(s)" - previously a single native `<select multiple>` listing every active user regardless of role - is now Task Stage-dependent: two Business-Role-filtered `.kcpc-model-picker` chip pickers (SHOOTING -> Camera Person, EDITING -> Video Editor, reusing `camerapersonUsers`/`videoEditorUsers` and the exact `model-picker.js` wiring already used for Planning's Cameraperson(s)/Model(s) pickers) sit in the form together; new `src/main/resources/static/js/reassign-form.js` shows only the one matching the current Task Stage `<select>`, clearing the OTHER picker's selection (unchecking every box and firing `change` so its chips clear too) on every stage switch, so a stray Cameraperson can never ride along into an Editor reassignment or vice versa - all client-side, no reload. The current active assignee(s) for each stage are pre-checked (`shootingAssignments`/`editingAssignments`, same `isAssigned`-style nested `c:forEach`+`c:set` idiom already used elsewhere in this JSP) per the user's own suggested default. "Confirm Reassignment" stays disabled until the active picker has >=1 checked assignee AND Reason is non-blank. While implementing the UI filter, found the server itself never validated a new assignee's Business Role at all - `AdminActionService#reassign` accepted any active user id for either stage. Added a new private `requireBusinessRole(User, String expectedRoleName)` there, called for every new assignee in both the SHOOTING and EDITING branches, so a direct API call reassigning e.g. a Video Editor onto SHOOTING is now rejected server-side too, not merely hidden from the picker - the UI filter alone was never sufficient. Not in the frozen ERD/API spec (UI/UX plus a closed authorization gap, no schema change). | Explicit user request with a screenshot of the existing flat, unfiltered multi-select: "reassign m jo stage select h uske according assignes ki list aani chahiye... SHOOTING select -> sirf Camera Person, EDITING select -> sirf Video Editor... Stage change hote hi purani assignee selection clear ho. Assignee list immediately refresh ho, page reload na ho. Multi-select chip picker use karo... Current assignee ko list me dikhao but already selected/marked state ke saath [suggestion, latitude given]... Confirm Reassignment tabhi enable ho jab stage + at least one assignee + reason filled ho... Important: filtering Designation se nahi, Business Role se hi ho, same pattern jo aap Model/Assignment pickers me use kar rahe ho." The user's explicit PUBLISHING example in their message doesn't apply - `TaskStage` (`src/main/java/com/kcpc/mkt/workflow/domain/TaskStage.java`) only has SHOOTING/EDITING, matching the screenshot's own dropdown; Reassign was never scoped to Publishing, and adding a third stage there was out of scope for this request. Regression-guarded by new `HighPriorityEdgeCaseTest.reassignRejectsANewAssigneeWithoutTheMatchingBusinessRoleButAcceptsAMatchingOne` (a Video Editor reassigned onto SHOOTING is rejected, no `ShootingAssignment` created; the same call with a correctly-roled Camera Person succeeds and becomes the sole active assignment) - the first test this endpoint had at all. Full suite (85 tests) green; packaged build verified clean. |
| ENG-055 | Publishing's "Record Actual Publication Event" - previously a single-row form (pick one Planned Output, one Publication Target, one Event Type, submit, repeat per pair) - is now a bulk execution checklist: one row per real `(PlannedOutput, PublicationTarget)` pair from Planning's Publication Scope (`PlannedOutputPublicationTargetMappingRepository#findByPlannedOutput`, queried per INDIVIDUAL output, not the reelGroupId-grouped representative `outputTargetMappings` already used elsewhere on this page - each Reel Type of a REEL group, and each distinct Publication Target within one platform e.g. two different Facebook Pages, is genuinely its own separate task, matching the user's explicit data rule). A pair currently designated N/A is left off the checklist entirely (not merely unchecked); a pair with a live `ORIGINAL` event already recorded shows completed and read-only (evidence rendered as a link, a "Completed" status pill, no checkbox) so it can never be resubmitted from this screen. New `DeliverableMvcController.PublishingChecklistRow` (a small nested read-only view-model, plain getters per `ENG-003`'s established no-Lombok convention) and `#buildPublishingChecklist` build the `publishingChecklist` model attribute, duplicating (read-only) the same N/A-latest-record logic `PublishingService`'s private `latestNaAction` already uses internally, since that method isn't exposed. New `POST .../publishing/events/bulk` (`recordBulkEvents`) accepts three parallel lists (`plannedOutputIds`/`publicationTargetIds`/`evidenceUrls`, one shared `actualPublicationTimestamp` for the whole batch, defaulting to today) and loops calling the EXISTING single-row `PublishingService#recordActualPublication` per row - no new service method, no new business logic, since that method already had every rule this feature needed (PUBG-only, `PERM_08`+active-assignee, non-blank evidence, and critically the "auto-complete Publishing once every mapped pair is live-or-N/A" `isScopeResolved` check the user asked for, which turned out to already exist and just needed to keep firing per-row as before). Deliberately NOT one atomic transaction across the batch - each row succeeds or fails independently (partial success returns a `"N task(s) recorded... M task(s) could not be recorded: ..."` flash summary), matching `ENG-041`'s precedent that a batch-of-independent-items endpoint accepts partial failure as a simplification, unlike `ENG-045`'s single-form combined-submit which needs full atomicity because its fields aren't independent. `publishing-checklist.js`: checking a row's box `disabled=false`s that row's hidden `plannedOutputId`/`publicationTargetId` inputs and its Evidence URL field (disabled fields are never submitted by the browser, which is what keeps the three parallel lists in perfect lockstep with zero index-misalignment risk - no row's fields are EVER toggled independently of its own checkbox); "Submit Published Tasks" stays disabled until >=1 row is checked and every checked row's Evidence URL is filled; Select All toggles every pending row at once. A collapsed (`<details>`, matching this page's existing Reschedule/Reassign/Cancel pattern) "Record a Repost / Manual Entry" section keeps the OLD single-row form fully intact underneath the new checklist - Repost (a second, deliberately-allowed event for an already-live pair) isn't something the bulk checklist handles at all, and removing that capability entirely was never asked for. While implementing this, found `recordActualPublication` had no guard at all against a second `ORIGINAL` event for an already-completed pair - only the checklist UI would ever have hidden that checkbox, not the server. Added the check there directly (rejects with "already exists ... use Repost instead", `REPOST` events unaffected) - the true source of the user's explicit "Already completed task dobara submit na ho" rule, not just the new screen. Not in the frozen ERD/API spec (new bulk endpoint/view-model, no schema change - reuses `ActualPublicationEvent`/`PlannedOutputPublicationTargetMapping`/`PublicationTargetNaRecord` exactly as they already existed). | Explicit user request with a full worked mockup and an example table: "jo output planned apan ne planning section m kiya use hum publishing wali screen per show karwakar ek ek task ko check kar sabhi task ko select karke submit kar sakte h... Planning ke targets automatically yahan load hon... Har selected target ke liye required Evidence URL fill kare... Submit par sirf selected tasks complete hon. Unselected tasks Pending rahen... Already completed task dobara submit na ho. Jab saare required Publication Targets complete ho jayen tab Publishing stage automatically complete ho... Ek row ko sirf Planned Output mat samjho. Actual publishing task ideally: Planned Output x Publication Target hona chahiye... Kyuki ek channel publish ho sakta hai aur doosra abhi pending reh sakta hai," with a worked REEL-VERY_SHORT-to-two-different-Facebook-Pages example matching exactly what `buildPublishingChecklist`'s per-individual-output, per-mapping-row iteration produces. Regression-guarded by new `WorkflowVariantsE2ETest.publishingChecklistBulkSubmitRecordsMultiplePairsAndAutoCompletesOnTheLastOne` (checklist page shows 2 Pending rows and a disabled Submit button before anything is recorded; a lone `ORIGINAL` for one pair keeps the plan at PUBG and a second `ORIGINAL` for that SAME pair is rejected with "already exists"; bulk-submitting the remaining pair through the real `/publishing/events/bulk` POST shape transitions the plan to PP; the page afterward shows 2 Completed rows and 0 Pending). Full suite (86 tests) green; packaged build verified clean. |
| ENG-056 | The Publishing checklist's shared "Actual Publication Date" field (`ENG-055`) is removed entirely - `POST .../publishing/events/bulk` no longer takes an `actualPublicationTimestamp` param at all; `DeliverableMvcController#recordBulkEvents` stamps every row in the batch with `Instant.now()` (computed once before the loop, so every row submitted together shares the exact same timestamp) instead of parsing a user-supplied date. The old single-row "Record a Repost / Manual Entry" fallback form (`ENG-055`'s collapsed `<details>` section, `POST .../publishing/events`) is UNCHANGED and still has its own explicit Actual Publication Date field - this removal is scoped to the bulk checklist path only, where every row is genuinely being recorded at submit time, not the manual/Repost path where backdating a correction can be a real need. Not in the frozen ERD/API spec (no schema change - `ActualPublicationEvent.actualPublicationTimestamp` itself is untouched, only where its value comes from for this one endpoint). | Explicit user follow-up, same session as `ENG-055`: "Actual Publication Date ko hata do wo by default current time stamp k according data lelega jab submit karne per" - remove the field, default to the current timestamp at submit time. Regression-guarded by updating `WorkflowVariantsE2ETest.publishingChecklistBulkSubmitRecordsMultiplePairsAndAutoCompletesOnTheLastOne`'s bulk POST to no longer send `actualPublicationTimestamp` at all (previously asserted the field's presence indirectly by sending it - now proves the endpoint works correctly without it). Full suite (86 tests) green; packaged build verified clean. |
| ENG-057 | "My Work" redesigned to match a detailed employee-dashboard mockup (dark navy header/branding already matched the existing theme; the new work was the nav, the page layout, and the data behind it): (1) `fragments/nav.jsp` now branches on `accessClass` (already globally available via `MvcNavigationAdvice`) - `EMPLOYEE`-class users get a minimal 3-tab nav (My Work / My Ideas / Submit Idea; "My Ideas" links to `/app/my-work#my-ideas`, an anchor into the existing My Ideas section already on that page, rather than a new dedicated route - no per-user idea-list endpoint existed, and building one was out of scope), CEO/MM keep the full existing nav completely unchanged. Sign-out restyled from an underlined text link to a compact red pill (`.link-button` - red is otherwise reserved for primary/destructive actions in this app, and ending a session fits that pattern; confirmed via grep it's the only usage of that class, so no other page changed). (2) `my-work.jsp`'s page header gained a title/subtitle plus a read-only employee summary card (Role from `User.businessRole` - never Designation; a "Shoot Lead" field + badge shown only when the employee is actually Lead on >=1 of their own active Shoot assignments, computed server-side). (3) The old three separate Shoot/Edit/Publish tables collapsed into one "Active Work" table via a new normalized `web.mvc.dto.ActiveWorkItem` (plain class with getters, not a record - `ENG-031`'s established JSP-EL constraint) - Content ID, Content/Task (`Idea.title`), a real Priority pill (HIGH/MEDIUM/LOW, own CSS colors), a stage-appropriate Planned Date (Shoot/Edit/Live date depending which stage the row is), Lead (Shoot rows only, blank for Edit/Publish - no analogous concept exists there), a simplified color-coded Status pill (`Assigned`/`In Progress`/`In Review`/`Needs Changes` - the last one newly computed by checking whether a `REQUEST_REWORK` review-cycle decision exists for that exact gate on that workflow instance, distinguishing "never reviewed yet" SIP/ED from "sent back for rework" SIP/ED, which the raw `WorkflowStatus` alone can't tell apart), a clickable "Open Drive ↗" link (`target="_blank" rel="noopener noreferrer"`), and a "View Details" action linking straight to the EXISTING `/app/deliverables/{id}` page - not a new detail screen (see rationale below). "Completed Work" (renamed from "My Completed Work / History") gained a `contentPlanId` (previously absent - `CompletedWorkItem` had no way to link anywhere at all) enabling its own "View Details", a Content/Task title, and a Remarks column sourced from the deciding review cycle's `decisionReason`. (4) Fixed a real N+1 the mockup's detailed spec explicitly called out avoiding: `LandingMvcController#myWork` previously ran 2 queries PER completed row (`WorkflowTransitionHistoryRepository`/`ReviewCycleRepository`, both by single `WorkflowInstance`); now two NEW batched finder methods (`findByWorkflowInstance_IdInOrderByTransitionTimestampAsc`, `findByWorkflowInstance_IdIn`) fetch every relevant instance's data in exactly 2 queries total up front, grouped into `Map<UUID, List<...>>` in Java and read from for both the Active-row rework-detection AND the Completed-row logic - zero additional per-row queries either way. Deliberately did NOT build: a notification bell (no backend notification concept exists anywhere in this app - a fabricated static badge would be fake UI, not a real feature, so it was left out rather than faked) or a bespoke restricted "Active Task Detail Screen" (the existing `/app/deliverables/{id}` page already provides everything the mockup's detail-screen section describes for Shoot - Shoot Instructions already un-muted for real content (`ENG-049`), a Jira-style Comments thread with an own-comment-only "..." Edit/Delete menu and no-reload posting (`ENG-050`), and Start Shoot/Submit Shoot Review gated by both `PERM_08`-equivalent execution authority AND active-assignee status (`ENG-043`) - reusing it outright satisfies "reuse existing JSP/CSS structure, do not redesign the whole application" far better than forking a parallel page would; the one known gap is that a Cameraperson clicking View Details on a COMPLETED (past-Shoot) plan still sees that same page's later-stage panels read-only, since this app has no per-viewer-role panel hiding on that page today - closing that would itself be exactly the kind of broad redesign this task was told not to attempt). The mockup image's fifth status label ("Revised") wasn't implemented - the user's own WRITTEN spec listed exactly four example statuses (Assigned/In Progress/In Review/Needs Changes), which is what was built. Not in the frozen ERD/API spec (view/DTO/query-shape changes only, no schema change). | Explicit user request with an extremely detailed six-section spec and a generated mockup image: "Create a Cameraperson Employee 'My Work' screen... Use the same visual language as the current employee UI... Do not redesign the whole application. Reuse the existing JSP/CSS structure and current employee theme," plus explicit requirements for the summary card, Active/Completed Work tables with named columns, an "Active Task Detail Screen" description, explicit authorization rules ("Backend authorization must enforce these rules; UI hiding alone is not sufficient" - already true from `ENG-043`/`ENG-044`, reconfirmed rather than re-implemented), an explicit "must NOT see" list (management/admin controls, other stages' assignee details - satisfied by existing action-level permission gating plus the status-gated panel structure of `deliverable-detail.jsp`), and "Avoid N+1 queries when loading My Work tables" (the one item that was a genuine pre-existing bug, fixed as part of this work rather than left alone). Research was done via a dedicated read-only investigation pass before writing any code, given the size of the request, to confirm exactly how much of the mockup already existed (the header's dark-navy branding, active/completed data separation, Business-Role-based visibility) versus what was net-new (the nav's employee scoping, the unified table, the summary card, the N+1 fix) - deliberately narrowing scope to what was real and buildable within "reuse, don't redesign" rather than chasing every mockup pixel literally. Regression-guarded by updating the two existing `MyWorkVisibilityTest` cases (the heading-text split helper now looks for "Completed Work" instead of the old "My Completed Work / History", and the post-approval assertion no longer checks for the now-removed "SHOOT" stage-label substring, since a single employee's Active/Completed tables only ever contain rows for their own one Business Role in practice, making an explicit per-row stage column redundant - both tests still assert the core visibility/active-to-completed-transition rules, which are unchanged). Full suite (86 tests) green; packaged build verified clean. |
| ENG-058 | Second My Work iteration, superseding ENG-057's generic layout specifically for Camera Person employees (every other Business Role keeps ENG-057's plain Active/Completed Work tables unchanged - a `${businessRoleName == 'Camera Person'}` branch in my-work.jsp is the only fork point, one shared route/page/DTO set either way): (1) Four KPI cards (Active Shoots/Rework Required/Delayed/Completed) computed by FILTERING the exact same `activeWork`/`completedWork` lists ENG-057 already builds (`roleLabel == "Cameraperson"` / `stageWorked == "SHOOT"`), never a separate count query - the user's explicit "same backend source... so count/table mismatches cannot occur" is structurally guaranteed by construction, not by convention. (2) Client-side tabs (Active Work/History/Marks, `my-work-tabs.js` - pure show/hide of already-rendered panels, default Active Work, no fetch) replace the previous stacked-panels layout for this branch only. (3) `ActiveWorkItem`/`CompletedWorkItem` gained `models` (comma-joined `ContentPlanTalentEntry.talentName`, batch-loaded via a new `findByContentPlan_IdIn` call alongside the transition/review-cycle batches ENG-057 already added - avoids reintroducing N+1 for the new column), and `ActiveWorkItem` gained `delayDays`/`actionLabel`. Delay is computed independently of the status label (`plannedShootDate.isBefore(today) && not yet completed` - an In Review row can still be delayed from the employee's perspective) and rendered as its own "Delayed · N day(s)" pill overriding the normal status pill in the JSP, not baked into the status string itself, keeping the two concerns separable. `actionLabel` is a plain navigation label, not an inline form/POST - "Clicking the row/action should open the task detail screen" - so Start Shoot/Continue/Resume/View Feedback are just different link text pointing at the SAME existing `/app/deliverables/{id}` page; "In Review" gets `null` (no button rendered) per "no execution button, show status only." A delayed row that would read "Continue" reads "Resume" instead; a delayed-but-never-started row still reads "Start Shoot" (nothing to resume). (4) `deliverable-detail.jsp`'s Shoot panel gained two new blocks, both read-only, both sourced from data already available on that page (no new queries beyond one new helper): a compact "Content Summary" grid (Content ID/Name/Priority/Planned Shoot Date/Model(s)/Shoot Lead/Drive Link/My Status) - closes the gap `ENG-057`'s docs entry explicitly flagged ("the Content Summary fields aren't laid out in a single neat card") without redesigning the rest of the page; and a "Rework Feedback" box, shown only when the most recently DECIDED Shoot Review cycle was a Request Rework (new `DeliverableMvcController#latestDecidedReworkReason`, mirrors the existing `pendingCycle` helper's shape) - separate from the Comments discussion thread and from Timeline (which permanently keeps the same reason text as history regardless - the Rework Feedback box disappearing after approval doesn't erase anything, Timeline is untouched). Not in the frozen ERD/API spec (view/DTO/query-shape changes only, no schema change, no new business rule - the completion/rework/permission logic is 100% reused from `ENG-043`/`ENG-057`). | Explicit user follow-up in the same session, framed as "Update the existing... screen to match the latest approved UI direction" with a new mockup and an even more detailed six-section spec than `ENG-057`'s: named KPI cards with an explicit "same backend source" requirement, named tabs, a Shoot-specific column set (Model(s), Shoot Date) the user asked for a SECOND time, per-status action-button labels ("Assigned -> Start Shoot... Delayed but started -> Resume... Submitted for Review -> no execution button"), and an explicit new "Rework Feedback" requirement ("Do not force employee to search Timeline for the feedback"). Confirmed "Clicking the row/action should open the task detail screen" is stated directly under the Active Work table section, which is what justified treating Action as pure navigation rather than building new inline POST endpoints - re-reading the spec's own structure rather than assuming. Scoped narrowly to Camera Person specifically (not all three stages) since that's literally the request's title and every example in it (KPI card icons/labels, table columns, the mockup image) is Shoot-only; extending the same KPI/tabs treatment to Editor/Publisher was explicitly out of scope for this request and was not attempted. Regression-guarded by a new `MyWorkVisibilityTest.kpiCountsMatchTablesAndReworkFeedbackShowsThenClearsAfterApproval` (KPI card counts asserted via exact substring match against the rendered card markup at three points in a rework-then-approve cycle: 1 active/1 rework/0 completed during rework, 0/0/1 after approval; Rework Feedback box present with the real decision reason text during rework, gone - but Timeline's own copy of that same reason deliberately left untouched and unchecked - after approval) plus updating the two pre-existing `MyWorkVisibilityTest` cases' heading-search helper (`"Completed Shoot Work"`, not ENG-057's generic `"Completed Work"`, since the fixture user is always Camera Person and now gets this branch). Full suite (87 tests) green; packaged build verified clean. |
| ENG-059 | New dedicated "My Ideas" screen for EMPLOYEE-class users at the same `/app/ideas` route the CEO/MM Idea Queue already uses, branched server-side (`IdeaMvcController#queue` dispatches to a new private `myIdeas(...)` when `accessClass == EMPLOYEE`, matching the established `/app/my-work`/`/app/pipeline` same-route pattern) - replacing ENG-057's `my-work.jsp#my-ideas` anchor stopgap with a real page (`nav.jsp`'s "My Ideas" link updated accordingly; the redundant panel removed from `my-work.jsp`). (1) `IdeaRepository.findBySubmittedByOrderBySubmittedAtDesc(User)` is the single source for both the table and all 5 KPI cards (Total/Under Review/Approved/Needs Changes/Rejected) - counts are computed by filtering that same in-memory list BEFORE search/status/date-range filtering or pagination is applied, so a count/table mismatch is structurally impossible, matching the "same backend source" rule ENG-058 already established. (2) A 5-way status-label mapping (new `statusLabel(WorkflowStatus)` in `IdeaMvcController`) reduces the full downstream workflow status set to what an idea submitter actually cares about: `PA`->"Under Review", `RJ`->"Rejected", `RET`->"Needs Changes", `PL`->"Approved", everything else (`PLRV`/`SA`/.../`COMP`)->"In Planning" - confirmed via `ContentPlan`'s constructor that an approved idea's `WorkflowInstance` is the SAME instance used for the entire downstream pipeline, so its status must be bucketed, not shown raw. (3) Search (`q` against title/`businessIdeaCode`, case-insensitive contains) + status filter + submitted-on date-range filter (7/30/90/all days) + pagination (6/page) are implemented as plain `GET` query params re-rendering the full page - deliberately NOT AJAX, since no client-side table-filtering/pagination JS pattern exists anywhere else in this codebase to reuse safely; `<c:url>`/`<c:param>` used for pagination links specifically so the free-text query is URL-encoded automatically. New `fn:escapeXml` taglib import (first use in this codebase) safely echoes the raw query back into the search input's `value` attribute (plain `${...}` EL does not auto-escape). (4) Feedback column shows the latest DECIDED `IDEA_REVIEW` cycle's `decisionReason` verbatim, or literal "—" when none exists - never fabricated. (5) `Idea` has no `category` field at all (confirmed by reading the entity), so "do not show Category anywhere" required no removal, only not adding one. (6) View Details (`idea-detail.jsp`) gained an explicit Idea Details block (ID/Title/Description/Submitted By/Submitted On/status pill), a "Review Feedback" panel (shown only when feedback exists), and a "Status History" panel restricted to idea-lifecycle events only - a new `IDEA_LIFECYCLE_TRIGGER_COMMANDS` constant (`SUBMIT_IDEA`/`APPROVE_IDEA`/`REJECT_IDEA`/`RETAIN_IDEA`/`REOPEN_IDEA`) filters `WorkflowTransitionHistory` down to just those, excluding all downstream production-stage transitions, per "do not expose internal management-only information unnecessarily." No employee-facing correction/resubmit control was added for a Retained (`RET`) idea: `IdeaService.reopen()` requires `PERM_01_IDEA_REVIEW` (CEO/MM-only) and has no employee-authorized equivalent anywhere in the codebase, so the spec's conditional ("if existing workflow allows employee action, show controls") resolves to showing nothing - confirmed by reading `IdeaService` in full, not assumed. (7) Backend authorization: `IdeaMvcController#detail` now checks `idea.getSubmittedBy().getId().equals(currentUser.getId())` for EMPLOYEE-class viewers and treats a denied/nonexistent idea identically - both `redirect:/app/ideas` with a flash `errorMessage`, never idea content. While implementing this, found and fixed a real pre-existing bug: an uncaught `DomainException` thrown from any plain `@Controller` (MVC/JSP) handler - confirmed via live `curl` against the running dev server hitting the already-existing `DeliverableMvcController#requirePlan`'s identical uncaught-throw pattern - produces a raw HTTP 500 with the full exception stack trace exposed in the response body, because `RestExceptionHandler`'s `@ExceptionHandler(DomainException.class)` only applies to `@RestController` responses; no MVC-side equivalent existed. `IdeaMvcController#detail` was rewritten to look up the idea via `.orElse(null)` and redirect-with-flash-message (the same `RedirectAttributes` house pattern every POST handler in this codebase already uses) instead of throwing, for both the not-found and ownership-denied cases - the data was never actually at risk (no idea content leaked either way), but the error-handling path is now consistent with the rest of the app instead of an inconsistent, unprofessional 500. This MVC-500-on-uncaught-`DomainException` gap is pre-existing and codebase-wide (not newly introduced), and is NOT otherwise fixed globally by this change - only this one call site. Not in the frozen ERD/API spec (view/DTO/query-shape/routing changes only, no schema change, no new business rule). | Explicit user request with a full mockup-driven spec: page header + KPI cards with the same "same backend source" requirement as ENG-058; search/status/date filters; an explicit **"Important: Do not show Category anywhere on this screen"**; exact table columns with per-column formatting guidance including **"Do not invent feedback"**; a View Details screen (Idea Details/Review Feedback/Status History, the latter restricted to "major idea workflow events only... Do not expose internal management-only information unnecessarily"); pagination "Showing 1 to 6 of 12 ideas" style; and an explicit, security-framed authorization section: **"Only show ideas submitted by the logged-in employee. Employee must not be able to access another employee's ideas by changing the URL or ID. Enforce this at backend authorization level as well."** That last sentence is what drove the 500-vs-404/redirect investigation and fix - a security requirement phrased around backend enforcement is not satisfied by an ownership check that technically blocks the data but leaks a stack trace and behaves inconsistently with the rest of the app, so the fix was treated as in-scope for this task rather than a separate pre-existing-bug ticket. Regression-guarded by a new `MyIdeasVisibilityTest.myIdeasShowsOnlyOwnIdeasWithMatchingKpiCountsAndBlocksDirectUrlAccessToAnotherEmployeesIdea`: two Employee fixtures each submit one idea, Employee A's `/app/ideas` is asserted to contain only their own title (never the other's) with KPI counts matching, Employee A's own idea detail page renders normally (200), a direct URL/ID guess at Employee B's idea from Employee A's session asserts `302` + `Location` containing `/app/ideas` + response body never containing the other idea's title (proving the fix, not just the pre-existing data-safety), and CEO's own Idea Queue is confirmed unaffected (still sees both). Full suite green; packaged build verified clean. |
| ENG-060 | Submit Idea screen (`idea-submit.jsp`) rebuilt to match a new mockup, splitting the old single "Reference Link / Note" field (which mixed a URL and free text in one column) into 4 distinct fields: Idea Title (unchanged, `title`), Reference Link (`referenceLink`, now URL-only, validated both client- and server-side), Additional Note (new), and Idea Description / Details (unchanged `notesRemarks`, relabeled). The frozen `ideas` table (ERD-TBL-009) only had 2 free-text columns, so a new nullable `additional_note` column was added via `V19__ideas_additional_note.sql` after confirming with the user which of two options they wanted (a real new column vs. UI-only merging two fields into the existing single column) - user chose the new column. `Idea`'s constructor, `IdeaService.submit` (gained an `additionalNote` param plus a new server-side URL check - `java.net.URI` must be absolute with an `http`/`https` scheme, rejecting with `DomainException.badRequest("Reference Link must be a valid URL")`), `IdeaSubmissionRequest`/`IdeaResponse` (REST DTOs, `API-OP-011`) all gained the new field as a purely additive, backward-compatible change - no existing caller (MVC or REST) breaks by omitting it. `idea-detail.jsp` gained an "Additional Note" row and its "Reference / Note" row was split into a real clickable Reference Link (target=_blank), matching the same separation on the submit side. Client-side (`idea-submit.js`): live `N / 120` and `N / 500` character counters (Title/Description only - Additional Note has no stated limit in the spec, so none was added); required-Title and valid-URL-Reference-Link validation that blocks submission and shows an inline `.field-error` message directly below the field without a page reload (existing `.field-error`/`.input-error` classes/pattern reused from `planning-submit.js`/`review-decision.js`, not invented new); errors clear live the moment the field becomes valid; Reset intercepts the `reset` event and asks for confirmation only when any field actually has content, otherwise resets immediately. On the backend side, `IdeaMvcController#submit` on a `DomainException` now re-renders `idea-submit` (200, not a redirect) with every entered value (`title`/`referenceLink`/`notesRemarks`/`additionalNote`) echoed back into the form via `fn:escapeXml`/`c:out`, plus a new `errorField` model attribute (`"referenceLink"` if the message mentions Reference Link, `"title"` otherwise) so the JSP places the backend-authoritative error under the right field too, not just as a generic banner - satisfying "if backend validation fails, preserve submitted values and return field-specific validation messages instead of losing the form state" for the no-JS-fallback/JS-can't-fully-replicate case. On success, `redirectAttributes.addFlashAttribute("successMessage", "Idea submitted successfully.")` before `redirect:/app/ideas` - `my-ideas.jsp` already renders `successMessage` (`ENG-059`), so this is "redirect to My Ideas according to the existing application flow" with the success message surfaced there, without adding a second "View in My Ideas" link/screen. No Category field exists or was added. No React; all vanilla JS/JSP, consistent with every other screen. Not in the frozen API spec beyond the additive DTO field (schema change logged here per the `ENG-033` precedent for out-of-ERD additions). | Explicit user request with a full mockup and an exhaustive field-by-field spec, including exact placeholders/helper text/character limits per field and an explicit "Do not combine link and note in the same field" instruction that directly named the old field's actual defect. Since the mockup's 4-field layout didn't fit the existing 2-column schema, the user was asked (via a direct question, not assumed) whether to add a new column or merge fields in the UI only - chose the new-column option, which is what was built and is the cleanest fit for "Additional Note" and "Idea Description / Details" being genuinely separate concepts with separate helper text/limits in the spec. The validation-UX section was explicit and detailed ("Do not refresh the page when client-side validation fails," "Prevent form submission... Keep all entered values... Show inline error directly below the invalid field... Focus/scroll to the first invalid field," "Remove the inline error immediately once the field becomes valid," and separately "Backend validation must still remain authoritative... preserve submitted values and return field-specific validation messages instead of losing the form state") - read literally as two separate requirements (client-side UX AND backend-authoritative re-validation with state preservation), both implemented rather than treating client-side validation as sufficient on its own. Regression-guarded by a new `SubmitIdeaFormTest` (3 tests): a full-fields submission persists `additionalNote` and `notesRemarks` as genuinely separate values and redirects to My Ideas; an invalid Reference Link is rejected by the backend (200, not a redirect - proving the idea was never persisted) with the exact entered title/link/note/description all still present in the re-rendered page body; a blank title is likewise rejected with the other entered values preserved. Full suite (91 tests) green; packaged build verified clean. |
| ENG-061 | My Ideas screen and the shared `idea-detail.jsp` corrected to a genuine domain-model rule: the Idea lifecycle (Submitted -> Approved/Retained/Rejected/Reopened) is separate from Planning, so once an Idea is Approved its Idea Status must read "Approved" forever, never a downstream Planning/Shoot/Edit/Publishing workflow state name - closing a real gap in ENG-059's original mapping, which had bucketed every status past `PL` (Planning, Shoot Assigned, ... Completed) into a made-up "In Planning" label. `IdeaMvcController#statusLabel` is now `(WorkflowStatus, latestLifecycleTrigger)`: `RJ`->"Rejected", `RET`->"Retained" (renamed from ENG-059's "Needs Changes" - Retain is a genuinely different concept from "needs changes," and the mockup named it explicitly), `PA`->"Under Review" UNLESS the most recent idea-lifecycle transition was `REOPEN_IDEA`, in which case "Reopened" (PA alone is ambiguous - a brand-new submission and a just-reopened-from-Retained idea are both PA), and everything from `PL` onward (regardless of how far downstream) -> "Approved" unconditionally - this is the actual rule fix. The header's raw `${idea.workflowInstance.currentStatusCode.statusName}` badge on `idea-detail.jsp` (which had been silently leaking real Planning-stage names like "Shoot Assigned" at the very top of the page this whole time, pre-dating even ENG-059) was replaced with the same corrected `ideaStatusLabel`/`ideaStatusCssClass` pair used everywhere else on the page - the bug existed before this task but was directly in scope given "Do not use Planning workflow states as Idea statuses." A "Deliverable" panel already existed (from ENG-059) showing "This idea was approved and moved into Planning." + an "Open the deliverable" link whenever a `ContentPlan` exists for the idea; the sentence is kept (reworded to match the mockup's exact suggested text) as the allowed non-status informational note, but the operational link itself is now gated `${accessClass != 'EMPLOYEE'}` - an Employee gets the informational sentence only, never a click-through into the Planning/Shoot/Edit/Publishing operational page, while CEO/MM (who legitimately need to jump from an idea to its resulting deliverable) keep the link unchanged. "Idea Decision History" (renamed from ENG-059's "Status History") no longer renders raw `WorkflowTransitionHistory` from/to `WorkflowStatus` names either (the same leakage problem, just in the history list) - a new `web.mvc.dto.IdeaHistoryEvent` (plain class, ENG-031 pattern) re-labels each of the 5 idea-lifecycle trigger commands into "Submitted"/"Approved"/"Rejected"/"Retained"/"Reopened" via a new `lifecycleEventLabel(String)` switch, carrying the reason inline (`transitionReason`, so a Reject/Retain's reason is visible directly in history too, not just the top Feedback panel) - `IdeaMvcController#toHistoryEvents` maps the already-filtered, already-newest-first lifecycle list built by `ideaLifecycleHistory`. Fixed a genuine pre-existing N+1 while doing this rewrite (flagged by the new spec's own "Avoid N+1 queries for summary cards and table data"): `MyIdeaRow`'s feedback had been fetched via `reviewCycleRepository.findByWorkflowInstanceAndGateTypeOrderByCycleNumberDesc` called once PER idea inside the `.map()` building the table rows - now batched via the already-existing `findByWorkflowInstance_IdIn`/`findByWorkflowInstance_IdInOrderByTransitionTimestampAsc` repository methods (2 queries total for the whole table, grouped into `Map<UUID, List<...>>`, same pattern established in ENG-057/058), which is also what made the Reopened-vs-Under-Review distinction affordable at table scale (the latest-lifecycle-trigger lookup piggybacks on the same batched transition-history fetch). KPI cards: Total/Under Review/Approved/Retained/Rejected (5 cards, matching the mockup exactly) computed from the same `allRows` list as before ENG-059's rule; a Reopened idea is folded into the "Under Review" count (per the spec's own explicit fallback: "either include it under Under Review... if that matches the existing backend semantics" - it does, a Reopened idea is, again, awaiting a fresh review decision) while still being independently filterable/displayed as its own "Reopened" badge on the row and in the status filter dropdown (5 options: Under Review/Approved/Retained/Rejected/Reopened - "In Planning" removed entirely, matching "Do not show... In Planning... as Idea Status values"). Two new small, additive `kcpc:istDate`/`kcpc:istTime` EL functions were added alongside the existing shared `kcpc:ist` (untouched, still used everywhere else in the app) so My Ideas' Submitted On column could render date/time on two lines per the mockup without changing the single-line format every other screen in the app relies on. No Category anywhere (still no such field on `Idea`). Not in the frozen ERD/API spec (view/DTO/query-shape changes only, no schema change - deliberately scoped to `my-ideas.jsp`/`idea-detail.jsp`/the Employee-facing status vocabulary; `idea-queue.jsp`, the CEO/MM management list view, was deliberately left untouched and still shows the raw, full-precision `WorkflowStatus.statusName` per row, since that level of pipeline-progress detail is exactly what a management tracking view is for and this request was scoped to the Idea-submitter-facing screens). | Explicit user request with a full mockup and an explicit, standalone "Important Business Rule" section stating the Idea/Planning separation directly and listing exactly which values must never appear as Idea Status ("In Planning," "Planning Review," "Needs Changes," "Shoot/Edit/Publishing states"), plus an explicit allowance for a small non-status informational note when an idea has moved to Planning with the exact suggested wording used here, and an explicit "Reopened Behavior" section spelling out the PA-ambiguity requirement in domain terms ("Idea Status = Reopened... employee should see the latest review feedback / reason"). Regression-guarded by a new `MyIdeasVisibilityTest.ideaStatusStaysApprovedThroughPlanningAndReopenedIsDistinguishedFromUnderReview`: an idea is approved and its resulting plan is driven all the way to Shoot Assigned (SA) via the real Planning-review flow, asserting the Idea Status stays "Approved" and the string "Shoot Assigned"/"In Planning" never appears anywhere on either the My Ideas table row or the idea's own detail page at any point; the informational "moved to Planning" sentence is asserted present for the employee with NO "Open the deliverable" link, while the same idea viewed as CEO DOES show the link; a second idea is Retained (asserting the "Retained" badge + reason), then Reopened via the CEO REST endpoint (asserting a distinct "Reopened" badge, not "Under Review", with the prior Retain reason still visible as the latest feedback, and an "Idea Decision History" entry reading "Reopened by..."), then asserts the Under Review/Approved/Retained KPI counts are exactly what the two ideas' real statuses should produce. Full suite (92 tests) green; packaged build verified clean. |
| ENG-062 | Camera Person "My Work" screen updated to a new mockup, backend workflow/routes/permissions/assignment rules/review-cycle logic left entirely unchanged (pure view-layer + one new read-only batched query path). (1) Active Shoot Tasks status vocabulary relabeled for clarity, shared by `LandingMvcController#activeStatusLabel`/`statusCssClass`/`actionLabel` (used by every Business Role's My Work, not just Camera Person - a pure text rename, same CSS classes, same underlying `WorkflowStatus`/rework-decision logic): `"In Review"` -> `"Submitted for Review"`, `"Needs Changes"` -> `"Rework Required"` (the `"Needs Changes"`-keyed KPI-count filter and `actionLabel` switch updated to match; no test asserted the old literal strings, confirmed by grep before renaming). Content ID in the Active Shoot Tasks table is now a link straight to `/app/deliverables/{id}` (`.content-id-link`, bold blue) - "make Content ID visually clear" - additive navigation only, no new authorization surface (same route, same guard). (2) "My Review Feedback" completely redesigned for the Camera Person branch only (the generic, all-gate-types table used by every other Business Role is unchanged, just relocated from outside the `<c:choose>` into the `c:otherwise` branch so it still renders identically for them): scoped strictly to decided `SHOOT_REVIEW` `ReviewCycle`s, grouped one card per Content ID (never IDEA_REVIEW/PLANNING_REVIEW/EDIT_REVIEW/Publishing data - satisfies the explicit "do not show unrelated gate data" requirement structurally, not by filtering a shared list). Scope is determined via the **existing** `ShootingExecutionParticipant` snapshot rows (a new `ShootingExecutionParticipantRepository.findByCameraperson(User)` finder - the entity itself, and its "queryable actual recorded shoot participants" purpose, already existed for self-review-conflict verification; this is its first UI-facing use) rather than "currently actively assigned" - deliberately, so a Cameraperson later reassigned off a plan keeps seeing their own historical feedback on it. Every historical decision is preserved and shown, never overwritten: the latest decision renders prominently, any earlier ones (rework loops) sit inside a native `<details>`/`<summary>` "View Feedback History" disclosure - zero JS needed for the expand/collapse (matches the progressive-enhancement house pattern better than inventing a new toggle script). "Decided by {name}" only appends "(Lead)" when the actual reviewer for that specific decision is provably the plan's own currently-active Shoot Lead (`reviewerId.equals(lead.getId())`) - never assumed. Search (Content ID contains) + status pills (All/Rework Required/Approved, filtering on each group's LATEST decision) + pagination are plain `GET` query params on the same `/app/my-work` route (new optional `feedbackQ`/`feedbackFilter`/`feedbackPage` params, all defaulted so every existing caller/test omitting them is unaffected) - same established plain-GET-reload pattern as My Ideas (ENG-059), no Export action added (no existing reusable export flow fit this narrow scope, and the user's own instructions explicitly said not to invent one). Batched (2-3 queries total for the whole section, not per-card): `ContentPlan` rows re-fetched by ID via the repository (never touching the LAZY `ShootingExecutionParticipant.contentPlan` association's fields directly, since this controller isn't `@Transactional` and open-in-view is disabled - only `.getId()` is safe off that proxy), `ReviewCycle`s via the already-batched `findByWorkflowInstance_IdIn`, and reviewer `User` rows batch-fetched by ID (`ReviewCycle.reviewer` is LAZY too) rather than dereferenced off the cycle's own proxy - a real `LazyInitializationException` was hit and fixed during implementation from exactly this pattern (caught by `MyWorkVisibilityTest` before it ever reached production). Not in the frozen ERD/API spec (view/DTO/query-shape changes only; the one new repository method is an additive finder on an existing entity, no schema change, no new business rule, no workflow transition touched). | Explicit user request with a full mockup and an exhaustive spec, repeatedly emphasizing backend/workflow preservation ("keep the current backend workflow, routes, permissions, assignment rules, review-cycle logic... unchanged", "Do not create new backend statuses just for UI labels", "Do not execute workflow transitions directly from the My Work table", "Do not create a second feedback database structure") alongside specific UI asks (friendly status labels with literal before/after examples, a Content-ID-grouped feedback layout instead of "an audit-log-looking table", multi-cycle history preservation with a named "View Feedback History" affordance, a reviewer-is-Lead label that must be data-true not assumed, and an explicit instruction not to add an Export button absent an existing reusable flow). The employee-summary-card's "Shoot Lead: current Shoot Lead name" / "LEAD badge only if the logged-in employee is the current Shoot Lead" bullets were read as describing the EXISTING ENG-057/058 behavior (which already shows the viewer's own name only when they are themselves a Lead) rather than requesting a change to whose name is shown when the viewer is not a Lead - the mockup screenshot's appearance already matches current behavior exactly, and building a "whose Lead do we show across multiple tasks with different Leads" design wasn't otherwise specified, so it was left as-is rather than guessed at. Regression-guarded by a new `ShootFeedbackTest.reworkThenApproveKeepsFullHistoryVisibleUnderTheCorrectContentIdAndLinksToTheDeliverable` (drives a real Idea->Planning->Shoot rework->resubmit->approve cycle over HTTP; asserts the Active Work row reads "Rework Required"/"View Feedback" during rework; asserts the feedback section shows the correct Content ID, the real rework reason text, an `REWORK REQUIRED` badge, and a working `/app/deliverables/{id}` link; asserts the page contains neither `IDEA_REVIEW` nor `PLANNING_REVIEW` nor `EDIT_REVIEW`; asserts that after the later Approve, both the latest `APPROVED` decision AND the earlier rework reason are still present via `View Feedback History` - proving history isn't lost) plus the three pre-existing `MyWorkVisibilityTest` cases re-verified green after the relabeling and the new batched-query path (this is also what caught and led to fixing the `LazyInitializationException` described above, before this task could be considered done). Full suite (93 tests) green; packaged build verified clean. |
| ENG-063 | Camera Person My Work's "My Review Feedback" section (ENG-062) moved to live physically INSIDE the `data-tab-panel="active"` `<div>` in `my-work.jsp`, as the last child after the Active Shoot Tasks table, instead of as a standalone section rendered after all three tab panels. No JS change needed: `my-work-tabs.js` already toggles the `.hidden` class purely on each `[data-tab-panel]` element itself (`panels[j].classList.toggle('hidden', ...)`), so anything nested inside `data-tab-panel="active"` hides/shows together with Active Shoot Tasks automatically - History and Marks tabs never render feedback at all now, matching "History aur Marks tab me feedback section bilkul hide rahe." Fixed a test that broke as a direct, correct consequence of this move: `MyWorkVisibilityTest.camerapersonTaskMovesFromActiveToCompletedHistoryOnceShootIsApproved`'s `splitOnHistoryHeader`-based assertion had implicitly assumed everything before the "Completed Shoot Work" heading was Active-Work-table-only content; once feedback (which legitimately still shows a just-approved decision regardless of whether that task has since left the Active Work table) sits in that same region, the assertion needed to check the Active Shoot Tasks TABLE specifically (new `activeShootTasksTableRegion` helper, isolating the substring between the "Active Shoot Tasks" and "My Review Feedback" headings) rather than the entire pre-History-header half of the page - the underlying invariant (task leaves the active table once Shoot Approved) is unchanged and still asserted, just via a more precise region. | Explicit user follow-up in Hinglish: "history ya marks tab se feedback hatana h... My Review Feedback sirf Active Work tab me dikhna chahiye... History aur Marks tab me feedback section bilkul hide rahe... My Review Feedback ko Active Work tab panel ke andar rakho, common page section ke bahar nahi." Regression-guarded by a new assertion added to `ShootFeedbackTest`'s existing test method: asserts the string index of `data-tab-panel="active"` < the index of "My Review Feedback" < the index of `data-tab-panel="history"` in the rendered page, proving the structural nesting (not just visual behavior) directly - plus the pre-existing `MyWorkVisibilityTest`/`ShootFeedbackTest` assertions all re-verified green after the fix. Full suite (93 tests) green; packaged build verified clean. |
| ENG-064 | Camera Person's Shoot Task Detail screen redesigned as a genuinely new, dedicated view (`shoot-task-detail.jsp`) rather than reskinning the shared multi-stage `deliverable-detail.jsp` shell in place - same route (`GET /app/deliverables/{id}`), same `DeliverableMvcController#view` GET handler and its existing model-building (reused wholesale: `plan`/`status`/`user`/`talentEntries`/`shootingAssignments`/`shootComments`/`canCommentOnShoot`/`isShootActiveAssignee`/`openHold`/`timeline`), just a different returned view name when `user.getBusinessRole().getRoleName().equals("Camera Person")` **and** the plan's status is in the Shoot-relevant window `{SA, SIP, SRV, SAP}` - matching the established "same route, branch server-side by role" house pattern (`/app/my-work`, `/app/pipeline`, `/app/ideas`). Outside that window (Planning, Edit, Publishing, etc.) every role - including a Camera Person who navigates there via a stale link - gets the unchanged standard shell, so no other screen's behavior changed. New additive-only model attributes, built by a new `addShootTaskDetailAttributes` helper: (1) `shootFriendlyStatusLabel`/`shootFriendlyStatusCssClass` - a small local 4-way mapping (`SA`->Assigned, `SIP`->In Progress or Rework Required, `SRV`->Submitted for Review, `SAP`->Approved) purely for display, never a new `WorkflowStatus`; (2) `shootDelayDays` - the same "planned Shoot Date in the past and not yet SAP" computation `ActiveWorkItem` already uses on My Work, recomputed here since the page's existing generic `delayed` flag is Live-Date-based, not Shoot-Date-based; (3) `shootProgressSteps` - a new `List<ShootProgressStep>` (5 fixed nodes: Assigned/In Progress/Review/Rework Required/Approved), each carrying a `done`/`current`/`pending` state and an optional real `WorkflowTransitionHistory` timestamp (`ACTIVATE_SHOOTING`/`START_SHOOTING`/`SUBMIT_SHOOT_REVIEW`/`REQUEST_REWORK_SHOOT`/`APPROVE_SHOOT` respectively, latest occurrence of each) - a node's date is `null`, and simply not rendered, whenever that transition never happened, so no fake dates are ever shown; "Rework Required" is `current` only when `status==SIP` and the latest decided SHOOT_REVIEW cycle was a rework (reusing the exact boolean the pre-existing `shootReworkFeedback` computation already implies) and is otherwise always `pending`, never `done`, since it is explicitly a transient UI flag, not a persisted milestone; (4) `shootFeedbackLatest`/`shootFeedbackPriorHistory` - reuses the **existing** `ShootFeedbackEntry` DTO one-for-one (the same class My Work's "My Review Feedback" section already uses, ENG-062/063), built here for this single plan's decided `SHOOT_REVIEW` cycles only (never Idea/Planning/Edit Review), newest-first, with the same reviewer-batch-fetch pattern (`ReviewCycle.reviewer` is LAZY and this controller isn't `@Transactional` - a real `LazyInitializationException` was hit here too, same root cause and same fix as ENG-062/063, caught by the pre-existing `MyWorkVisibilityTest` before this task was considered done) and the same "only label a reviewer `(Lead)` if they provably are the plan's actual active Shoot Lead" rule. The template itself: breadcrumb + friendly status card + progress tracker (display-only, confirmed no `<form>`/POST anywhere near it) + a 65/35 two-column layout (Content & Shoot Information, Shoot Instructions read-only, Comments / Latest Reviewer Feedback with a native zero-JS `<details>`/`<summary>` "View Feedback History", the current task's single action button, a secondary "Open Drive Link" outline button, Timeline showing the latest 5 events with the rest behind another `<details>` "View Full Timeline"). Comments block is a byte-for-byte copy of the existing Shoot Comments markup (same CSS classes, same `data-comments-action`/edit/delete POST URLs) so the existing `stage-discussion.js` keeps working with zero JS changes - own-comment-only edit/delete, soft-delete, AJAX-enhanced with a working no-JS fallback, all unchanged. The action button reuses the exact existing endpoints unmodified (`POST .../shooting/start`, `POST .../shooting/review/submit`) - "Continue Shoot" is deliberately the SIP-status label for both a fresh in-progress task and a rework-returned one (per explicit instruction, "prefer Continue Shoot for consistency"; both submit to the same, only, existing Submit-for-Shoot-Review endpoint - no second "resolve rework" flow was invented). None of the CEO/MM-only controls (Planning/Shoot Review decision forms, qualifying-recipient picker, any assignment/reassignment/reschedule/cancel/hold management) exist anywhere in this new template at all - not hidden by a permission flag, simply never written, so there is no risk of a CSS/JS bug ever exposing them to a Camera Person. Fixed one CSS-specificity issue proactively while building the primary action button (recurring pattern this session): `.app-main button[type="submit"]` would otherwise have overridden the intended full-width primary button with the app-wide compact/right-aligned default; resolved with a `.shoot-action-panel button[type="submit"].shoot-primary-action` selector, matching the established `button.my-class` fix pattern from earlier in this session. Not in the frozen ERD/API spec (view/DTO/query-shape changes only; `ShootProgressStep` is a new plain view-model class, no schema change, no new business rule, no new endpoint, no workflow transition touched). | Explicit user request with a full mockup and an extremely detailed 16-section spec, opening with "without changing any existing workflow, backend rules, permissions, routes, status transitions, review logic, comments behavior, or data model" and closing with an explicit list of management controls a Camera Person must never see. Multiple sections repeated "do not invent"/"do not create a new X" (no fake dates, no new feedback storage model, no separate Resolve-Rework workflow, no new backend statuses for `Rework Required`) - each honored by construction (dates are `null`-checked real transition timestamps; feedback reuses the existing `ShootFeedbackEntry`/`ReviewCycle` data; the action button targets the one existing Submit-for-Review endpoint). The reviewer-is-Lead caveat ("Do not call reviewer Lead unless the reviewer actually is the Shoot Lead according to existing data") was implemented as a literal per-decision identity comparison rather than assumed, matching the identical caveat already honored for My Work's feedback section (ENG-062). Regression-guarded by a new `ShootTaskDetailTest.camerapersonSeesRedesignedPageThroughTheFullLifecycleWhileCeoSeesTheStandardShell`: drives a real Idea->Planning->Shoot Start->Submit->Rework->Resubmit->Approve cycle over HTTP, asserting at every status (SA/SIP/SRV/SIP-after-rework/SAP) that the Camera Person's page shows the correct friendly status, the correct/only available action button (or none), the "Rework Required"/"APPROVED" feedback badges with the real reason text, that no other gate's data leaks in, and that a prior rework decision remains reachable via "View Feedback History" after approval instead of disappearing; separately asserts the SAME plan viewed by CEO still renders the original, unchanged shared shell (no `breadcrumb` element present). Plus the pre-existing `MyWorkVisibilityTest.kpiCountsMatchTablesAndReworkFeedbackShowsThenClearsAfterApproval` updated to assert the new page's actual (intentionally changed) text - "Latest Reviewer Feedback"/"REWORK REQUIRED"/"APPROVED" instead of the old generic panel's "Rework Feedback" box - while still proving the same underlying behavior (the real reason text is shown during rework, and is never lost, now via "View Feedback History", after approval). Full suite (94 tests) green; packaged build verified clean. |
| ENG-065 | Shoot Task Detail's "Content & Shoot Information" restyled from the grid layout (`.content-summary-grid`) to a clean label\|value list (new `.info-list`/`.info-row` CSS, divider line between rows, label left/value right, reusing the existing `.summary-field-label`/`.summary-field-value` classes) - pure JSP/CSS layout change on `shoot-task-detail.jsp` only, no model/controller/DTO change. Field set adjusted to match the explicit list given: Content Information now shows Content ID/Content Name/Priority/Planned Output/Reel Type as five separate rows (Planned Output and Reel Type were previously combined into one "REEL (VERY_SHORT)"-style line; the optional Category row from ENG-064's first pass is dropped, since it wasn't in the requested field list or the accompanying mockup); Shoot Information keeps Planned Shoot Date/Planned Live Date/Model(s)/Cameraperson(s)/Shoot Lead/Drive Link, with the Shoot Lead row now also carrying the same `.lead-badge` chip Cameraperson(s) already showed inline (visual consistency, "compact badges... for Priority/Lead"). Everything else on the page (data source, permissions, actions, comments, feedback, timeline) is untouched. | Explicit user follow-up UI-only request ("Do not change any backend data, fields, workflow, permissions, or logic. This is a UI layout change only"), with a screenshot of the existing grid layout and a precise target field list per card. Regression-guarded by re-running `ShootTaskDetailTest`/`MvcScreenSmokeTest`/`MyWorkVisibilityTest` (JSP renders without error, no other assertions touch this section's exact markup) - a dedicated new test was not added since no behavior changed, only presentation; per this session's own established guidance, pure CSS/JSP layout changes are visually verified by the user rather than re-tested end-to-end here. |

| ENG-066 | Video Editor employee screens built as a genuine parallel to the Camera Person ones (ENG-057/058/062/063/064/065), reusing the shared infrastructure those already established rather than duplicating it: (1) My Work's Edit branch (`businessRoleName == 'Video Editor'`) - same 4-KPI-card + 3-tab (Active Work/History/Marks) layout, Active table columns `Content ID | Idea/Content | Priority | Edit Date | Editor(s) | Edit Lead | Status | Drive | Action` (the "Editor(s)" and "Edit Lead" columns are genuinely new - the Shoot table only ever showed "Model(s)" - so `LandingMvcController`'s Edit-assignment loop now computes them the same way the Shoot loop already computes Model(s)/Shoot-Lead: one `editingAssignmentRepository.findByContentPlanAndActiveTrue(plan)` call per active row, reused for both the Lead lookup and the comma-joined all-editors string, reusing `ActiveWorkItem`'s existing generic `models`/`leadName`/`shootLead` fields rather than adding new ones since they were already unused for Edit rows). Delay computation (past `plannedEditDate`, not yet completed) is now computed for Edit rows too (previously always `null`) - the same rule Shoot already had (ENG-058). "My Review Feedback" (EDIT_REVIEW-scoped, inside the Active Work tab panel only, own `editFeedbackQ`/`editFeedbackFilter`/`editFeedbackPage` query params so it doesn't collide with the Shoot version's) reuses the EXISTING `ShootFeedbackEntry`/`ShootFeedbackGroup` DTOs verbatim (both already gate-agnostic in shape, doc comments updated to say so) via a new `buildEditFeedback`/`toFeedbackEntry`(shared)/new `EditingExecutionParticipantRepository.findByEditor(User)` finder - same "participated, not just currently assigned" scoping rule as Shoot (ENG-062), same batched-reviewer-fetch fix already needed there. History tab: `Content ID | Content | Edit Date | Completed On | Result | Remarks | View`. Marks tab: unchanged, `myMarks` was already role-agnostic. The employee-summary-card gained an "Edit Lead" badge, parallel to the existing "Shoot Lead" one. (2) Edit Task Detail (`edit-task-detail.jsp`, new) - exact structural mirror of `shoot-task-detail.jsp` (ENG-064/065), rendered at the SAME `/app/deliverables/{id}` route/handler for `businessRoleName == 'Video Editor'` when status is in the Edit-relevant window `{EA, ED, ERV, EAP}` (EAP included defensively though never actually observed - `ERV->EAP->RFP` fires atomically in `EditingService#decideEditReview`, so unlike Shoot's SAP there is no resting "Edit Approved" status; once approved the Editor's page falls back to the standard shared shell, since RFP is Publishing's stage, not Edit's - a real, intentional, documented asymmetry from Shoot, not a bug). Deliberately reuses the same `.shoot-*` CSS classes (progress tracker, status card, action panel, feedback card) rather than duplicating them, so the two screens stay visually identical as the user explicitly wanted ("Shoot aur Editor screens almost identical rahengi"). New `addEditTaskDetailAttributes`/`editFriendlyStatusLabel`/`editFriendlyStatusCssClass` mirror `addShootTaskDetailAttributes` exactly, keyed off Edit's own trigger commands (`ASSIGN_EDITOR`/`START_EDITING`/`SUBMIT_EDIT_REVIEW`/`REQUEST_REWORK_EDIT`/`APPROVE_EDIT`) and reusing the shared `ShootProgressStep`/`ShootFeedbackEntry` DTOs (doc comments updated). Action button reuses the existing unmodified endpoints (`/editing/start`, `/editing/review/submit`) - "Continue Edit" for both fresh and rework-returned ED, no separate "resolve rework" flow. Comments block is byte-for-byte the existing Edit Comments markup/action URLs, so `stage-discussion.js` needs no changes. No CEO/MM management controls (Editor assignment, Edit Lead change, Reassign/Reschedule/Cancel/Hold, decision forms) exist anywhere in the new template - not hidden, simply never written, same as the Shoot page. Not in the frozen ERD/API spec (view/DTO/query-shape changes + one new repository finder only; no schema change, no new business rule, no new endpoint, no workflow transition touched). | Explicit user follow-up (Hinglish): "jaise hume shoot ki screen banai h wase hi hum editor ki banani h... Workflow bilkul existing hi rahega" with a full itemized structure (KPI cards, tabs, table columns, feedback columns, History columns, task-detail layout) and an explicit closing instruction that the two screens should look "almost identical" so terminology/data is the only thing that changes (Shoot->Edit, Cameraperson->Editor, Shoot Lead->Edit Lead, Shoot Review->Edit Review). Every new piece was built by mirroring the already-shipped, already-tested Shoot equivalent line-for-line rather than redesigning independently, which is both what was asked for and the lowest-risk way to extend proven code. The EAP/no-resting-Approved-status asymmetry was discovered by re-checking `EditingService.decideEditReview` (already known from this session's own earlier workflow-reference research) rather than assumed, and handled by simply letting the existing view-name-branch fall through to the standard shell once RFP is reached, instead of inventing a fake "still shows Edit Task Detail" state that would misrepresent the real Publishing-stage status. Regression-guarded by two new tests mirroring the Shoot ones exactly: `EditFeedbackTest.reworkThenApproveKeepsFullHistoryVisibleUnderTheCorrectContentIdAndLinksToTheDeliverable` (rework->resubmit->approve cycle over HTTP for a real Editor, asserting the friendly labels, the real reason text, the Content ID link, no other gate's data, and the tab-panel-only placement) and `EditTaskDetailTest.editorSeesRedesignedPageThroughEaEdErvReworkThenFallsBackToStandardShellAfterApproval` (drives EA->ED->ERV->rework->ED->approve, asserting the redesigned page and its action buttons/status at every step, no management controls, CEO still sees the standard shell throughout, and - the one deliberate difference from Shoot - the Editor also falls back to the standard shell once RFP is reached). Full suite (96 tests) green; packaged build verified clean. |

| ENG-067 | New "My Shoots" screen for Model employees (`/app/my-shoots`, `my-shoots.jsp`), mirroring the mockup: page title, "{Full Name} • {BUSINESS ROLE}" subtitle line, a 2-KPI-card row (Upcoming Shoots count, Next Shoot date), Upcoming/Past tabs, and a table (`Content ID | Idea/Content | Shoot Date | My Role | Other Talent | Status | View`). Required a real identity link first: `ContentPlanTalentEntry` (ERD-TBL-041) previously stored Models/Talent as a free-text name string only, with no link back to the actual `User` the Planning UI's Model(s) picker had selected - not safely scoped-by-identity for a "my own shoots only" screen (the same privacy rule, SRS-REQ-067, already applied to My Work/My Ideas). Asked the user directly (`AskUserQuestion`): add a real nullable FK vs. match on exact free-text name; user chose the FK. `V20__talent_entry_user_link.sql` adds a nullable `content_plan_talent_entries.talent_user_id UUID REFERENCES users(user_id)` (+ index) - nullable because the frozen REST contract (API-OP-017/018) still accepts plain talent-name strings with no user behind them, but the MVC Model(s) picker (which only ever offers real Model-Business-Role users) populates it going forward. `ContentPlanTalentEntry` gained the `talentUser` association + a 3-arg constructor (the old 2-arg one had exactly one call site, `PlanningService`, so it was changed in place rather than kept as dead back-compat surface); new `ContentPlanTalentEntryRepository.findByTalentUser`. To carry the optional user link through the service layer without breaking the frozen REST DTO, added an internal-only `planning.dto.TalentSelection(String talentName, UUID talentUserId)` record (a legitimate `record` - never rendered by any JSP, so ENG-031's getter-only constraint doesn't apply) - `PlanningService.updateParameters/savePlan/savePlanAssignAndSubmit` now take `List<TalentSelection>` instead of `List<String> talentNames`; `ContentPlanRestController` converts the frozen `List<String> talentNames` wire field into `TalentSelection`s with a null user (REST DTO itself untouched, wire contract preserved exactly); `DeliverableMvcController.resolveTalentSelections` resolves the MVC picker's real `modelUserIds` into `User`-backed `TalentSelection`s. `LandingMvcController.myShoots()`: scopes by `talentEntryRepository.findByTalentUser(user)` (never by name-matching), batch-fetches the linked plans and all-talent-per-plan (N+1-avoiding, same pattern as the rest of this controller), splits Upcoming/Past purely on `plannedShootDate` vs. today (`null` or `>= today` = Upcoming) - deliberately independent of the deliverable's downstream `WorkflowStatus`, since a Model's interest is the shoot day itself, not the content's full lifecycle. "My Role" shows the constant `"Model"` rather than the mockup's varied descriptive text ("On-screen Saree Draping Model" etc.) since there is no per-shoot role/description field backing that data anywhere in the ERD - consistent with this session's repeated "don't invent data" rule (ENG-059/060). "Status" shows the raw `WorkflowStatus.getStatusName()` (e.g. "Shoot Assigned", "Planning Approved") rather than a friendly per-stage relabel like the Camera Person/Editor screens use, since a Model isn't executing a specific stage - matches how the CEO Pipeline/Idea Queue already show raw status names to non-execution audiences, and matches the mockup's example values exactly. "View" links to the existing generic `/app/deliverables/{id}` shell (not a new task-detail page) since the mockup only covered the list screen and a Model holds no Shoot/Edit/Publish permission or assignment, so every actionable form on the generic page is already invisible to them. Nav (`fragments/nav.jsp`): a Model's first EMPLOYEE-tab link is "My Shoots" instead of "My Work" (Models hold no execution assignment, so "My Work"'s task-execution concept doesn't apply) - branched on a new global `businessRoleName` model attribute added to `MvcNavigationAdvice` (alongside the pre-existing `accessClass` one) so every view has it without each controller setting it manually. `my-work-tabs.js` (Active Work/History/Marks tab toggle) was generalized in place - rather than duplicated - to default to whichever `.my-work-tab` is server-rendered with the `active` CSS class instead of a hardcoded `"active"` `data-tab` value, so My Shoots' "Upcoming" tab can be the default without touching My Work's existing behavior (verified backward-compatible: My Work's own first tab already both has `data-tab="active"` and the `active` class). Two small additive CSS rules: `.kpi-cards-2` (2-column KPI grid, mirroring the existing `.kpi-cards-5`) and `.status-pill.status-neutral` (a single gray pill for the raw status label, since no per-status color mapping applies here). Not in the frozen ERD/API spec (one new nullable FK column + index, one new internal-only DTO, one new route/view, no new business rule, no workflow transition touched) - the schema addition specifically was the one part explicitly confirmed with the user before implementing. | User sent a "My Shoots" screen mockup (dark navy header, "My Shoots" nav tab, 2 KPI cards, Upcoming/Past tabs, the exact table columns/example rows) with the instruction "make model screen" - no further clarifying text. Implementation followed this session's established defaults for anything the mockup didn't fully specify (raw status text, generic role label, date-based tab split, reuse of the generic task-detail shell) since those are consistent, previously-applied precedents from this same build, not this feature's own one-off inventions; the one genuinely new/risky decision - adding a schema-level identity link - was raised explicitly to the user rather than assumed, because a wrong guess there (e.g. fragile name-matching) would have been a real privacy/correctness bug, not just a cosmetic mismatch. Regression-guarded by a new `MyShootsTest.modelSeesOnlyOwnLinkedShootsSplitIntoUpcomingAndPast`: drives two real Content Plans through Planning with different Model(s) selections and shoot dates (one future via standard schedule, one explicitly past via urgent schedule), then asserts as a real logged-in Model that (a) the nav shows "My Shoots" not "My Work", (b) both own-linked plans' Content IDs appear, (c) the co-talent's name appears under "Other Talent" and the viewer's own name never does, (d) the raw "Planning" status label renders, (e) a third Model with no linked plans sees neither plan and both empty-state messages, and (f) a non-Model employee still sees "My Work", not "My Shoots". Full suite green (97 tests: 96 pre-existing + this one); the pre-existing `PlanningSingleFormTest`/`PlannedOutputsTableTest`/`CeoPipelineDashboardTest`/`GoldenEndToEndFlowTest`/`MvcScreenSmokeTest` were re-run first to confirm the `TalentSelection` signature refactor across `PlanningService` didn't regress any existing Model(s)-selection or CEO Pipeline "Models" column behavior, before writing any new code. |

| ENG-068 | Publisher employee screens built as a third parallel to the Camera Person/Video Editor ones (ENG-057/058/066), reusing the shared infrastructure those established rather than duplicating it - but genuinely Publishing-flavored, not a mechanical copy, since Publishing has no review/rework gate at all. (1) My Work's Publisher branch (`businessRoleName == 'Publisher'`) - same visual shell, but only 3 KPI cards (Active Publishing / Pending Targets / Completed - no Rework card, since there's no review gate to need rework from) and only 2 tabs (Active Work/History - no Marks tab, since no marks model is defined for Publishing). Active table: `Content ID | Content | Priority | Planned Live Date | Publisher(s) | Targets | Status | Drive | Action` - "Targets" is a new kind of column (`ActiveWorkItem.targetsSummary`, a new nullable field added to the existing shared DTO, null for Shoot/Edit rows) showing "resolved / total" (Planned Output × Publication Target) pairs, computed by a new `PublishingService.summarizeTargets(ContentPlan)` (public method, reusing the existing private `hasLivePost`/`latestNaAction` helpers already used internally for the PUBG→PP auto-transition check - same resolution rule, exposed for display). "Pending Targets" KPI is a genuinely different shape of number from every other KPI in this app - a sum of unresolved pairs across every active row, not a row count - accumulated in the same loop that builds each row's own Targets column so the KPI and the table can never drift apart. Fixed a latent bug while touching this: the Active Work table's per-row action-button text was hardcoded `"Start Shoot"` for every role's "Assigned" state (`LandingMvcController.actionLabel`) - silently wrong for Editor rows too, not just the new Publisher rows; `actionLabel` now takes the row's `roleLabel` and returns "Start Shoot"/"Start Edit"/"Start Publishing" correctly per role. (2) Publishing Task Detail (`publish-task-detail.jsp`, new) - structural mirror of `shoot-task-detail.jsp`/`edit-task-detail.jsp` (same `.shoot-*` CSS classes, same route/handler, `businessRoleName == 'Publisher'` + status in `{RFP, PUBG, PP}`), but with real content differences the user explicitly asked for: no "Latest Reviewer Feedback" panel anywhere (Publishing has no review gate, so there's nothing to show), and a 5-step progress tracker with Publishing's own milestones - "Assigned → Ready for Publishing → Publishing → Scope Resolved → Performance Pending" - instead of Shoot/Edit's Assigned/In Progress/Review/Rework Required/Approved shape. Two of those milestones needed a different data source than Shoot/Edit's pattern: Publisher assignment triggers no workflow transition at all (the plan is already at RFP before and after - unlike Shoot/Edit Assignment, which IS the trigger that moves PLAP→SA / SAP→EA), so "Assigned"'s own date comes from `PublishingAssignment.getAssignedAt()`, not `WorkflowTransitionHistory`; and "Ready for Publishing"'s date is found via a new `latestTransitionToStatus(ascending, toStatus)` helper (filtering by the transition's *resulting* status rather than a specific trigger command name), since RFP can be entered by more than one trigger command depending on path. The "Scope Resolved"/"Performance Pending" pair fire on the exact same instant in this system (`PUBLICATION_SCOPE_RESOLVED` both resolves scope and reaches PP atomically) so both steps share one timestamp and neither is ever marked "current" - same asymmetry precedent as Edit's EAP (ENG-066), just named as two tracker nodes because the user explicitly listed both. The "Publication Targets" checklist and "Actual Publication Events"/Evidence-URL history reuse the shared shell's already-built `publishingChecklist`/`events` model attributes and the checklist's exact markup/element IDs from `deliverable-detail.jsp`, so the existing `publishing-checklist.js` needed zero changes; Comments are likewise byte-for-byte the existing Publishing comments markup/action URLs, so `stage-discussion.js` needed no changes either. **The one deliberate divergence from Shoot/Edit's pattern:** PP is included in the relevant-status window (unlike Edit, which falls back to the standard shell once RFP is reached) - Publishing has no next stage waiting to take over the way Edit hands off to Publishing, so PP genuinely is the Publisher's own final resting status, and the redesigned page keeps rendering there instead of reverting to the generic shell. No CEO/MM management controls (Publisher assignment, Publishing Description edit, Target N/A, evidence correction) exist anywhere in the new template - not hidden, simply never written, same as Shoot/Edit. Not in the frozen ERD/API spec (view/DTO/query-shape changes + one new public service method + one new DTO field + one new small helper; no schema change, no new business rule, no new endpoint, no workflow transition touched). | Explicit user follow-up (Hinglish): "publisher ki screen ui bhi shoot or editor k jase banegi kya?" answered by the user themself with a full itemized structure (3 KPI cards named explicitly, 2 tabs explicitly excluding Marks, the exact Active table columns, the task-detail section list, the explicit 5-step progress flow spelled out end-to-end, and the explicit rule "Review Feedback section nahi hoga, kyunki Publishing ka koi ReviewCycle nahi hai"). Every structural piece was built by mirroring the already-shipped, already-tested Shoot/Edit equivalents (lowest-risk way to extend proven code, matching this session's established approach), while every place Publishing's real workflow shape differs (no review gate, no Lead concept, assignment doesn't trigger a transition, PP is a genuine resting state not a hand-off) was implemented to match the ACTUAL `PublishingService`/`WorkflowStatus` behavior confirmed by reading the code, not assumed from the Shoot/Edit template - consistent with this session's "verify against real workflow-reference code, don't assume" rule (already applied once this session to Edit's EAP asymmetry). Regression-guarded by a new `PublisherScreenTest.publisherSeesRedesignedPageThroughRfpPubgToPerformancePending`: drives a real Content Plan through Shoot→Edit→RFP over HTTP, assigns a Publisher, asserts My Work's KPI cards/table/Targets-column/action-button text and the redesigned page's breadcrumb/progress tracker/no-feedback-panel/no-management-controls at RFP, records one of two mapped Publication Target events and asserts the partial "1 / 2 resolved" figure matches identically in both the task-detail page and the My Work table, records the second event and asserts the plan reaches PP, and asserts the redesigned page KEEPS rendering at PP (the one deliberate divergence from Edit) while the row correctly falls from Active Work into History on My Work. Also fixed a real regression this change surfaced in the pre-existing `WorkflowVariantsE2ETest.publishingChecklistBulkSubmitRecordsMultiplePairsAndAutoCompletesOnTheLastOne`: it counted `status-pill` occurrences across the ENTIRE page body as a Publisher's own view, which broke once that Publisher's own view became the redesigned page (with its own extra top-level status pill) instead of the generic shell - fixed by scoping the count to just the checklist table's own markup (a `checklistTableHtml` helper), which is what the test actually intended to assert and is now immune to any other pill elsewhere on the page. Full suite green (98 tests: 97 pre-existing + this one); packaged build verified clean. |

| ENG-069 | Content Pipeline (`/app/pipeline`, CEO/MM) redesigned into a workflow-focused management dashboard - UI/JSP/CSS only, zero change to `PipelineDashboardService`'s existing query/join logic, `WorkflowTransitionService` usage, permissions, or the one-row-per-Content-ID contract. Page header gained a subtitle ("One row per Content ID · All dates in YYYY-MM-DD") and 5 KPI cards (Planning/Shoot/Edit/Publishing/Attention-Delayed) - pure display counts grouped by each row's own friendly status text, computed once in `LandingMvcController#pipeline` over the same rows the table renders (never a second query, never a new backend status). The 20-column flat table was reorganized into 6 visually-grouped column headers (Content/People/Planned Dates/Actual Dates/Publication/Current) with pastel group-header backgrounds; Reference Link/Category/Head (Actor) were dropped as separate always-visible columns (still fully intact in `PipelineRow`/the backend response, just not rendered on this condensed view - `View Details` still shows everything) and Channels/Platforms moved into a "Publication" group after the date groups - both explicitly confirmed with the user (`AskUserQuestion`) since they broke `CeoPipelineDashboardTest`'s exact header-sequence assertion; user chose to update the test to match the new intentional design rather than keep the old columns hidden-but-present. Two genuinely new pieces of row data were added, both purely additive/display-only: `PipelineRow.priority` (surfaces `ContentPlan.getContentPriority()`, already-stored data never previously exposed on this DTO) and `PipelineRow.delayed`/`delayDays` (a new `PipelineDashboardService.delayDays(status, plan, today)` - compares whichever Planned date applies to the plan's CURRENT stage - Shoot/Edit/Live for the SA-SIP-SRV / EA-ED-ERV / RFP-PUBG windows respectively - against today, mirroring `LandingMvcController`'s existing per-employee "past planned date, not yet past that stage" rule, just evaluated at the whole-plan level; Planning/terminal/already-resolved statuses are never flagged). Never a persisted or workflow status - BFD's only real "delayed" catalogue entry is the supplementary `DLY` flag, untouched. Cameraperson(s)/Model(s)/Video Editor(s)/Platforms now render as a compact "first name +N" chip (`fn:split`/`fn:trim`/`fn:length` over the existing comma-joined display strings) with the full list still in the `title` tooltip - preserves every name/platform string the old flat-text columns exposed, just visually condensed. Status is now a color-coded pill grouped by pipeline stage (`stage-planning`/`stage-shoot`/`stage-edit`/`stage-publishing`/`stage-performance`/`stage-completed`/`stage-cancelled`) with the raw friendly status text unchanged inside it (still exactly `WorkflowStatus.getStatusName()`, per `doesNotContain(">PP<",">COMP<",">CAN<")`); a delayed row additionally shows a small red "Delayed · N day(s)" note under the badge. Performance state relabels the verbose default "Not Yet Applicable" to "Not Due" in the JSP only (backend field itself unchanged) - the other 3 raw values (Pending/Updated/Completed) render as-is, already single-word/compact and matching the user's requested vocabulary for 3 of 4 states. Action column is a single "View Details →" link to the existing `/app/deliverables/{id}` route - explicitly no Start Shoot/Start Edit/Start Publishing/Submit buttons anywhere on this management dashboard, matching the user's explicit "not employee task execution" rule (those already only ever existed on the employee-facing My Work/Task Detail screens, never here, so this is a non-change made explicit). Content ID stays sticky-left; Status and Action are now sticky-right (`position: sticky; right: 0` / `right: 6.5rem`) so they stay visible while horizontally scrolling the wide table - the responsive strategy is horizontal scroll (already-existing `.pipeline-scroll`), the simpler of the two options the user explicitly allowed ("allow horizontal scrolling, or collapse..."). Drive Link icon (`aria-label="Open Drive Link"`/`.pipeline-link-icon`, byte-for-byte the same SVG/markup as before) moved inline next to the Content ID rather than its own column, since the user's explicit column spec never named a separate Drive Link column. | User supplied a full text redesign spec plus a generated mockup screenshot, explicitly framed as "UI redesign only" with an extensive "Important Flow Rules" list of things NOT to change (statuses, `WorkflowTransitionService`, assignment rules, review/rework logic, permissions, CEO/MM native authority, active-assignee execution gates, one-row-per-Content-ID, routes, tests) and an explicit closing instruction to run the pipeline/dashboard/authorization tests afterward to confirm workflow behavior is unchanged. The one place the new column-group spec structurally conflicted with an existing test (dropped/reordered columns vs. `CeoPipelineDashboardTest`'s exact header-sequence assertion) was raised directly to the user rather than silently resolved either way - a wrong silent guess would have meant either quietly breaking an explicit "preserve existing tests" instruction or shipping a compromised, duplicated-looking UI the user never asked for; user chose to update the test. Every other design gap the mockup/spec left open (delay-indicator rule, KPI grouping windows, team/publication chip compaction, performance-state relabeling) was filled by directly mirroring an already-established precedent elsewhere in this same codebase (My Work's per-stage delay rule, the raw-status-text convention from My Shoots/CEO Pipeline, "don't invent data" for anything not backed by a real field) rather than invented fresh. Full suite green (98 tests: 97 pre-existing + `PipelineFilterSortTest` from the very next request landed in the same session - see ENG-070); `CeoPipelineDashboardTest` explicitly re-verified against the new column set/order. |
| ENG-070 | Content Pipeline gained real server-side filtering and sorting on the existing `/app/pipeline` route (query params only - no new route), replacing the client-side JS row-hide/paginate approach ENG-069 had shipped with. Sortable columns: Content ID, Priority, Planned/Actual Shoot/Edit/Live Date, Status - each header is a link (`fragments/pipeline-sort-header.jspf`, statically included once per sortable column to avoid repeating the link-building/toggle-direction/indicator markup by hand 9 times) that toggles asc/desc on repeat clicks of the same column, defaults to ascending on a fresh column, and shows a small ▲/▼ indicator on whichever column is currently active. Filters: Priority/Status/Performance State/Platform/Channel as `<select>` dropdowns (Status/Platform/Channel options are the distinct values actually present across the full unfiltered row set, computed once in the controller, so a dropdown never offers a choice that would return zero rows); Cameraperson/Model/Video Editor as free-text case-insensitive substring inputs (matched against the same comma-joined display strings `PipelineRow` already exposes - there's no ID-based multi-valued field to bind a dropdown to without a much larger schema-aware rewrite of `PipelineDashboardService`'s batch-join architecture, which was out of scope); a Delayed/Attention checkbox (reuses ENG-069's `PipelineRow.isDelayed()`); and Planned/Actual date-range pickers, both keyed off each row's own Live Date (Planned Live Date / Actual Live Date respectively) - the one date every row has regardless of which stage-specific date would otherwise apply, matching the date ENG-069's top-bar date-range control already targeted. All of this lives in a new `PipelineFilterCriteria` record (internal-only carrier, never JSP-rendered, so ENG-031 doesn't apply) and a new `PipelineDashboardService.filterAndSort(rows, criteria)` method that operates purely over the already-built `List<PipelineRow>` from the UNCHANGED `buildRows` - filtering/sorting is a display/reporting-layer concern over pre-joined rows, not a workflow rule, and never touches `buildRows`, its queries, or any business-logic method. Global search (`q`) still matches Content ID/SKU/Idea, unchanged in meaning from ENG-069, just moved from client-side JS to the same server-side `matches()` check as every other filter. Pagination became real (page/size query params, default 10/page) - the server now slices the filtered+sorted list and renders ONLY that page's rows, instead of ENG-069's client-side approach which always sent every row's HTML to the browser regardless of filters; this is the actual "large datasets stay efficient" win, even though the filter/sort computation itself still evaluates the full in-memory row list built by the existing single-query `buildRows` (a deliberate, documented tradeoff - true SQL-level WHERE-clause filtering across the joined multi-valued Camerapersons/Models/Platforms relations would mean rewriting `PipelineDashboardService`'s carefully N+1-avoiding batch-load architecture, which every instruction this session explicitly said not to touch). KPI cards intentionally keep reflecting the FULL unfiltered dataset (a stable at-a-glance company total), not the current filter scope - not requested by this task and safer left matching ENG-069's already-shipped behavior. A compact "Filters" drawer (`.pipeline-filters-drawer`, toggled by a small vanilla-JS show/hide - `pipeline-dashboard.js` no longer does ANY row filtering/pagination logic, that's 100% server-side now) holds the 11 filter fields instead of a dropdown embedded in every table column header, per the user's explicit instruction; the "Filters" button shows an active-filter count badge. Every sort-link/pagination-link preserves the rest of the current filter state via one shared `filterQueryString` (built once per request in the controller with `UriComponentsBuilder`, encoded) rather than each of the ~15 links individually repeating all 14 filter params by hand. | Explicit user follow-up: "Add proper filtering and sorting to the Content Pipeline without changing backend workflow or business logic," listing the exact sortable columns, the exact filter dimensions, keeping global search, an explicit preference for "a compact top Filters panel/drawer instead of placing a filter dropdown inside every table column," and an explicit preference for "server-side query parameters using the existing MVC/JSP pattern so large datasets remain efficient" - directly superseding ENG-069's client-side-JS interim approach for this specific concern (filtering/sorting/pagination), which was reasonable UI-only scope in ENG-069 but not what this follow-up asked for. The "server-side but still evaluates all rows in memory before pagination" tradeoff (rather than a full SQL-level rewrite) was a considered engineering call made without re-asking the user, since redesigning `PipelineDashboardService`'s query/join shape would itself violate the same message's "without changing backend workflow or business logic" / "Do not change... existing data semantics" constraints - the efficiency win requested (not shipping every row's HTML on every request) is fully achieved by real server-side pagination regardless of where the filtering computation itself happens. Regression-guarded by a new `PipelineFilterSortTest.filterSortAndPaginateWorkTogetherOverOwnFixtureRows`: creates its own uniquely-SKU-tagged fixture rows (immune to whatever else exists in the shared test database), asserts search scoping, priority-filter isolation, ascending/descending sort ordering by priority (with the sort-indicator markup present), 1-per-page pagination showing exactly one row per page with the correct "Showing X to Y of Z" text, a genuinely delayed fixture row (driven through real Shoot Assignment + Planning Review approval to reach Shoot Assigned status with a past Planned Shoot Date) correctly isolated by `delayed=true` while the 3 non-delayed fixture rows are excluded, and that an Employee is still redirected away even with filter query params present. `CeoPipelineDashboardTest` (both tests) re-verified green alongside it. Full suite green (99 tests: 98 pre-existing + this one); packaged build verified clean. |

| ENG-071 | Content Pipeline's filter UI redesigned again, per direct user follow-up: replaced ENG-070's single big Filters drawer with a per-column filter icon + popup in each relevant table header (matching a `Priority ↕ ⏷` mockup), and simplified the top bar back down to just Search + Reset Filters. All server-side query-param filtering/sorting from ENG-070 is preserved and extended, not replaced - still zero change to `PipelineDashboardService.buildRows`/`WorkflowTransitionService`/permissions/one-row-per-Content-ID. Column-by-column: Content ID gets sort only (no filter popup - top Search already covers Content ID/SKU/Idea substring matching); SKU and Idea each gain their OWN text-contains filter (new `PipelineFilterCriteria.sku`/`.idea` fields, new `sku`/`idea` sort options) in addition to being covered by global search, since the user explicitly listed them as separately filterable/sortable this time; Priority/Performance State are small radio-button popups (High/Medium/Low/All, Not Due/Pending/Updated/Completed/All); Cameraperson(s)/Model(s)/Video Editor(s) stay free-text substring filters, now in their own per-column popup instead of the drawer; Channels/Platforms (one visual column) gets ONE popup holding both the Platform and Channel `<select>`s together; Status keeps both its ENG-070 sort AND gains a filter `<select>` (the user's new column list didn't explicitly repeat "Sort" for Status, but nothing asked to REMOVE the sort capability ENG-070 had just added one request ago, so it was kept rather than treated as an unstated regression) - the Delayed/Attention checkbox was folded into the Status popup specifically (visually adjacent to the delay indicator that already renders under the Status badge) since it isn't a real table column of its own. The single biggest structural change: each of the 6 Planned/Actual date columns now has its OWN independent range filter (`plannedShootFrom/To`, `plannedEditFrom/To`, `plannedLiveFrom/To`, `actualShootFrom/To`, `actualEditFrom/To`, `actualLiveFrom/To` - 12 new query params replacing ENG-070's 2 combined `plannedFrom/To`+`actualFrom/To` pairs that both keyed off Live Date only) - `PipelineFilterCriteria` and `PipelineDashboardService.matches()` were extended accordingly, still pure in-memory filtering over the already-built row list, never touching `buildRows`. Every popup's form fields live inside ONE shared `<form>` wrapping the whole search-bar-through-table region (`#pipelineFilterForm`) rather than N separate per-popup forms with duplicated hidden fields - clicking "Apply" in any single popup submits every other column's current filter value along with it for free, since they're all fields of the same form; a small vanilla-JS module (`pipeline-dashboard.js`, rewritten again) only ever opens/closes one popup at a time (closes on outside click) and, for "Clear", resets that popup's own fields to empty/unchecked before letting the click's normal form-submit proceed - it never decides what rows are shown, that's 100% server-side per ENG-070's original resolution. A `PipelineFilterCriteria` field addition/reshuffle needed every one of its 26 positional constructor args re-ordered in the ONE call site (`LandingMvcController#pipeline`) - safe since it's a record with no other constructors/serialization concerns anywhere in the codebase (`AskUserQuestion` was not needed here; unlike ENG-069's column-drop conflict, this was a pure additive/reshaping change with no existing test asserting the old field order). Active-filter highlighting: each column's filter-icon gets an `.active` CSS class (`skuFilterActive`/`plannedShootFilterActive`/etc., one boolean per column computed once in the controller from the just-parsed request params) so a busy MM can tell at a glance which of the 15 popups currently has something applied - matching the mockup's explicit ask ("filter icon ko blue/highlighted dikhao"). | Direct user follow-up after seeing the ENG-070 drawer render in a real screenshot: "filter aise lagane k bajay hum har field per filter ka icon sorting ka icon laga sakate h na or uper reset ka" (why not a filter+sort icon per relevant column instead, and just Reset up top), followed by an explicit per-column capability table, a concrete header example (`Priority ↕ ⏷`), the exact popup interaction (radio list + Clear for Priority, From/To + Apply for date columns), an explicit ask for active-filter highlighting, and an explicit reconfirmation that server-side query params remain the right approach for correct pagination interaction. This directly supersedes ENG-070's drawer-shaped UI for the SAME already-server-side filtering feature (not a new capability, a UI-shape correction) - the underlying `PipelineFilterCriteria`/`filterAndSort`/pagination-slicing machinery from ENG-070 was extended in place rather than rewritten, since the user's complaint was specifically about the drawer's ergonomics, not the server-side approach itself (which they explicitly reconfirmed). Regression-guarded: `PipelineFilterSortTest` extended in place with new assertions for the per-column Planned Shoot Date range (isolating the existing delayed fixture row by a range that includes vs. excludes its real Planned Shoot Date) and for the new per-column popup markup being present (`data-popup-target="popup-sku"`, `id="popup-plannedShoot"`, `id="popup-status"`, "Reset Filters" text) while confirming the old `pipeline-filters-drawer` class no longer appears anywhere on the page; all of ENG-070's original search/priority-filter/sort-ascending-descending/pagination/delayed-filter assertions were kept running unmodified in the same test and still pass. `CeoPipelineDashboardTest` (both tests) re-verified green. A follow-up user report ("Content ID ka title sticky nhi h jab ki uski value sticky h" - the header sticks left correctly but the column header/title doesn't) traced to `.pipeline-table { border-collapse: collapse; }` - a well-documented Chromium/WebKit bug where `position: sticky` breaks for a cell that must stick in BOTH axes at once (the Content ID header needs sticky-top, as part of the header row, AND sticky-left, as the pinned column - the plain data cells only ever needed one sticky axis, which is exactly why they were unaffected and the header alone broke). Fixed by switching to `border-collapse: separate; border-spacing: 0;` (same collapsed visual look, since only `border-bottom` is set per cell and no adjacent-cell borders were ever relying on collapse to merge) - a pure CSS fix, no JSP/Java change. Full suite green (100 tests: 99 pre-existing, `PipelineFilterSortTest` extended not added-to-count separately); packaged build verified clean. |

| ENG-072 | Fixed a real bug the user hit: selecting a new "per page" size (e.g. 10 → 25) on Content Pipeline did nothing. Root cause: `filterQueryString` (built once in `LandingMvcController#pipeline`, reused by every sort link/pagination link/the per-page selector's `onchange`) used to include its own `&size=...`, and several of its consumers ALSO appended their own `&size=...` afterward - producing a URL with the `size` param TWICE (e.g. `...&size=10&sortBy=...&sortDir=...&page=1&size=25`). `HttpServletRequest#getParameter` (what `@RequestParam` binding uses under the hood) returns only the FIRST value for a repeated query param, so the per-page selector's newly-chosen value was silently discarded in favor of whatever size was already active - the page always reloaded at the OLD size, looking like the control did nothing. Fixed by removing `size` from `filterQueryString` entirely (mirroring how `sortBy`/`sortDir` were already deliberately excluded from it) so every consumer - sort links, pagination links, the per-page selector - supplies its own single `size` value with no duplication possible. | Direct user report: "pagination m page size 10 se 25 select karne per bad nhi rhi h." Root-caused by tracing every place `filterQueryString` gets consumed in `pipeline.jsp`/`pipeline-sort-header.jspf` rather than guessing, since a silent-failure bug like this (no error, just "nothing happens") is exactly the kind that's easy to misdiagnose without checking the actual rendered URL shape. Regression-guarded: `PipelineFilterSortTest` extended with an assertion that the per-page selector's `onchange` URL and a representative column's sort-link `href` each contain the `size=` query param exactly once (not zero, not two) - a test that would have caught this bug before it shipped. Full suite green; packaged build verified clean. |

## Deferred (tracked, not silently dropped)

| ID | Item | Planned resolution |
|---|---|---|
| CORR-001 | **CLOSED.** `predefined_mark_corrections` (ERD-TBL-026), `publication_evidence_corrections` (ERD-TBL-027), `performance_metric_corrections` (ERD-TBL-028) implemented: `V12__correction_ledgers.sql` (physical tables + `ERD-CON-029/049/050/051` CHECK/trigger constraints), JPA entities/repositories, `IdeaService.correctPredefinedMarks` / `PublishingService.correctEvidenceUrl` / `PerformanceService.correctMetrics`, REST endpoints at the exact frozen paths (`API-OP-033/041/046`), and `CorrectionLedgerFlowTest` (2 tests: ledger chaining + original-record immutability + DB-trigger proof). | Closed. |
| DB-001 | **CLOSED (code/config-complete; role-split runtime behavior UNVERIFIED — DOCKER DAEMON NOT AVAILABLE).** `V12` added `trg_reject_update_delete()` (UPDATE/DELETE reject) across all 15 pre-existing append-only tables plus the 3 new correction ledgers. `V13__db_privilege_split_and_truncate_guards.sql` closed the remaining gaps: (1) `work_hold_records` hard-DELETE rejection (`ERD-CON-062` rule 9, a dedicated trigger since V9's existing trigger only covers the UPDATE lifecycle) - proven by `DbIntegrityEnforcementTest.workHoldRecordHardDeleteIsRejectedAtTheDatabaseLevel`; (2) a `FOR EACH STATEMENT` `TRUNCATE` guard on every append-only table (row-level `BEFORE` triggers never fire on `TRUNCATE`) - proven by `DbIntegrityEnforcementTest.truncateIsRejectedOnAppendOnlyTables`; (3) the actual Postgres privilege/GRANT split - `docker-compose.yml` now bootstraps a `kcpc_migrator` superuser (schema owner, Flyway-only) and `db/init/01_create_app_role.sh` creates a separate `NOSUPERUSER` `kcpc_app` runtime role; `V13`'s Part B (idempotent, existence-guarded) grants `kcpc_app` exactly `SELECT/INSERT` on append-only tables, `SELECT/INSERT/UPDATE` (no `DELETE`) on `work_hold_records`, and full DML elsewhere; `application.yml`'s `docker` profile now configures a separate `spring.flyway.*` datasource (migrator credentials) from `spring.datasource.*` (restricted `kcpc_app` credentials). The GRANT/REVOKE semantics themselves were verified locally against a throwaway non-superuser role (`kcpc_verify_restricted`, created and dropped in the same session, never touching the shared local `kcpc_app`) - confirmed `permission denied` on `UPDATE`/`DELETE`/`TRUNCATE`. **What remains genuinely unverified:** actually running `docker compose up --build` end-to-end so the real `kcpc_migrator`/`kcpc_app` split executes inside a container (no Docker daemon in this environment - see "Known gap — Docker build verification"); local dev/test (`kcpc_dev`/`kcpc_test`) intentionally keeps its pre-existing single-role `kcpc_app` setup (already a superuser predating this session) rather than mutating shared local infrastructure outside this task's scope - `V13`'s Part B correctly no-ops there (existence/self-grant guarded) and the trigger layer (Part A) is fully active and tested regardless. | Closed; residual Docker verification tracked under the existing Docker gap, not re-listed separately. |
| TEST-001 | **Substantially expanded, not yet exhaustive.** The suite has grown from the original 7 tests to 32 (see `docs/IMPLEMENTATION_STATUS.md` "Automated tests"). `WorkflowVariantsE2ETest` (8 tests) now covers every golden-path variant listed in the continuation prompt's Section 7 that is resolvable without a second concurrent client (Reject, Retain/Reopen, Planning Review rework, Urgent Planning, the exactly-5-day Standard/Urgent boundary, Hold/Resume blocking a review submission, the Performance Due Date gate, multiple publication events/Target N/A/reversal/Repost, and Completed-deliverable Reopen for Publishing). `PermissionBoundaryTest` (4 tests) adds a representative slice of Section 6's permission-denial/workflow-guard checklist (unauthorized Employee 403 on Idea Review and Planning execution, out-of-order Shooting/Publishing start 409, immediate session revocation on deactivation). What is still outstanding: negative-path tests for Cancel/Reschedule/Reassign individually, Editing-specific rework, and every one of the 17 governed permissions tested individually rather than via the representative pair exercised so far; true concurrency/race-condition tests (two simultaneous requests) are not attempted - this environment's tests are all single-client sequential HTTP calls. | Continue backfilling the remaining domain actions and add a dedicated concurrency test (e.g. two parallel Hold attempts) in a follow-up pass; the `TestApiClient`/repository-lookup pattern established across `GoldenEndToEndFlowTest`, `WorkflowVariantsE2ETest`, and `PermissionBoundaryTest` is reusable for this. |
